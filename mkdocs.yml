site_name: gtsom
site_description: General Topology Self-Organising Map — SOMs on arbitrary output space geometries.
repo_url: https://github.com/jt-ut/gtsom
repo_name: jt-ut/gtsom

theme:
  name: material
  features:
    - navigation.sections
    - navigation.top
    - content.code.copy
    - search.suggest
  palette:
    - scheme: default
      toggle:
        icon: material/brightness-7
        name: Switch to dark mode
    - scheme: slate
      toggle:
        icon: material/brightness-4
        name: Switch to light mode

plugins:
  - search
  - mkdocstrings:
      handlers:
        python:
          paths: [src]
          options:
            docstring_style: numpy
            show_root_heading: true
            show_source: true
            members_order: source
            separate_signature: true
            show_signature_annotations: true
            merge_init_into_class: true
  - mkdocs-jupyter:
      execute: false

nav:
  - Home: index.md
  - Tutorial: gtsom_mnist_tutorial.ipynb
  - API Reference: api.md

markdown_extensions:
  - admonition
  - pymdownx.highlight
  - pymdownx.superfences
