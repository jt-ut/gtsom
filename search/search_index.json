{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"gtsom","text":"<p>General Topology Self-Organising Map \u2014 a Python package for training Self-Organising Maps (SOMs) on arbitrary output space geometries, including hexagonal grids, rectangular grids, and data-driven Delaunay triangulations.</p>"},{"location":"#overview","title":"Overview","text":"<p>A Self-Organising Map projects high-dimensional data onto a low-dimensional output lattice while preserving topological structure \u2014 nearby neurons learn to respond to similar inputs. GTSOM extends the classical SOM by allowing any graph structure as the output space, defined either by a regular grid or by the geometry of the data itself.</p> <p>Key features:</p> <ul> <li>Hexagonal and rectangular grid topologies (<code>GTSOM.from_grid</code>)</li> <li>Data-driven Delaunay triangulation topology (<code>GTSOM.from_data</code>)</li> <li>Exponential neighbourhood bandwidth annealing</li> <li>Parallel prototype updates via numba (optional)</li> <li>Per-epoch learning history including MQE, per-prototype MQE, and BMU change rate</li> <li>Built-in lattice visualisation with multiple colouring modes</li> </ul>"},{"location":"#installation","title":"Installation","text":"<pre><code>pip install git+https://github.com/jt-ut/gtsom.git\n</code></pre> <p><code>gtsom</code> depends on vqlp for vector quantisation and recall. Install it first:</p> <pre><code>pip install git+https://github.com/jt-ut/vqlp.git\n</code></pre> <p>For parallel prototype updates (recommended for large datasets):</p> <pre><code>pip install \"git+https://github.com/jt-ut/gtsom.git[parallel]\"\n</code></pre>"},{"location":"#quick-start","title":"Quick Start","text":"<pre><code>import numpy as np\nfrom sklearn.datasets import make_blobs\nfrom gtsom import GTSOM\n\n# Generate some data\nX, y = make_blobs(n_samples=1000, n_features=2, centers=5,\n                  cluster_std=0.8, random_state=42)\nX = X.astype(np.float32)\n\n# Initialise a 10x10 hexagonal SOM\nsom = GTSOM.from_grid(\n    X,\n    shape=(10, 10),\n    coord_init='hex',\n    W_init='random',\n    random_state=42,\n    labels=y,\n)\n\n# Configure the learning schedule\nsom.compile(\n    rho_0=3.0,       # initial neighbourhood bandwidth\n    rho_f=1.0,       # final neighbourhood bandwidth\n    target_epochs=50,\n    n_jobs=-1,       # use all CPU cores (requires numba)\n)\n\n# Train\nsom.fit(X, n_epochs=100, labels=y)\n\n# Visualise\nfig = som.plot(color_by='labels')\nfig.savefig('som.png', dpi=150, bbox_inches='tight')\n\n# Map data to output coordinates\ncoords = som.transform(X)   # (N, 2) array of lattice positions\n</code></pre>"},{"location":"#documentation","title":"Documentation","text":"<p>Full documentation and a worked tutorial on MNIST handwritten digits is available at:</p> <p>https://jt-ut.github.io/gtsom</p>"},{"location":"#citation","title":"Citation","text":"<p>A paper describing GTSOM is currently under review. Citation information will be added here upon publication.</p>"},{"location":"#license","title":"License","text":"<p>MIT License. See LICENSE for details.</p>"},{"location":"api/","title":"API Reference","text":""},{"location":"api/#gtsom","title":"GTSOM","text":""},{"location":"api/#gtsom.GTSOM","title":"gtsom.GTSOM","text":"<pre><code>GTSOM(W, embed, recaller, kernel, random_state=None)\n</code></pre> <p>General Topology Self-Organizing Map.</p> <p>Do not construct directly for typical use \u2014 prefer the classmethods :meth:<code>from_grid</code> and :meth:<code>from_data</code>, which handle all initialisation internally and return a ready-to-fit instance.</p> <p>Parameters:</p> Name Type Description Default <code>W</code> <code>(array - like, shape(M, d))</code> <p>Initial prototype matrix.</p> required <code>embed</code> <code>Embedding</code> <p>Output-space embedding. Must already have coords, adjacency, and dist populated (i.e. <code>compute_topo()</code> already called).</p> required <code>recaller</code> <code>VQRecaller</code> <p>Nearest-prototype search object. Should be freshly constructed (unfitted); :meth:<code>fit</code> will call <code>update_BMU</code> each epoch.</p> required <code>kernel</code> <code>NeighborKernel</code> <p>Neighbourhood activation kernel.</p> required <code>random_state</code> <code>int or None</code> <p>Seed for all random operations. Stored as an instance variable and passed to any internal method that requires a random number generator.</p> <code>None</code> Notes <p>GTSOM does not perform any internal scaling of X. It is the caller's responsibility to whiten, standardise, or otherwise preprocess X before passing it to any method. Distances computed during prototype updates and BMU search will reflect the scale of the input features.</p> Source code in <code>src/gtsom/gtsom.py</code> <pre><code>def __init__(self, W, embed, recaller, kernel, random_state=None):\n    W = np.asarray(W)\n    if W.ndim != 2:\n        raise ValueError(f\"W must be 2-D, got shape {W.shape}\")\n\n    M_w = W.shape[0]\n    M_e = embed.coords.shape[0]\n    if M_w != M_e:\n        raise ValueError(\n            f\"W has {M_w} prototypes but embed has {M_e} neurons.\"\n        )\n\n    self.W = W\n    self.embed = embed\n    self.recaller = recaller\n    self.kernel = kernel\n    self.random_state = random_state\n\n    # DataValidator \u2014 set by classmethods after X is first seen;\n    # used to warn if a different X is passed to fit() or transform()\n    self._validator = None\n\n    # Annealing schedule and parallelism \u2014 set by compile()\n    self.rho_schedule = None\n    self.n_jobs       = None   # resolved thread count; None = all cores\n\n    # Fit state \u2014 populated during fit()\n    self.nbr_W = None\n\n    # Current training age (total epochs completed across all fit() calls)\n    self.age = 0\n\n    # Cumulative wall-clock time spent in fit() calls, in seconds.\n    # Accumulated with += across successive fit() calls.\n    self.train_time = 0.0\n\n    # Initial prototype matrix and embedding coords, stored at construction\n    # for reference (e.g. visualising how prototypes evolved from their\n    # starting positions). Never modified after initialisation.\n    self.W0 = None        # set by classmethods after W is finalised\n    self.coords0 = None   # set by classmethods after embed is finalised\n\n    # Previous-epoch BMU assignments, shape (N,), used to compute delBMU.\n    # Initialised to a vector of -1s by the classmethods (using N from\n    # DataValidator) so that delBMU = 1.0 at age=0 by construction,\n    # since -1 never matches a valid BMU index.\n    self.prevBMU = None   # set by classmethods to np.full(N, -1, dtype=int)\n\n    # Learning history: list of snapshot dicts, one per recorded epoch.\n    # learn_history_[0] is always the post-initialisation state (age=0).\n    # Subsequent entries are appended by fit() after each epoch.\n    # Each dict contains:\n    #   'age'    : int      \u2014 self.age at the time of the snapshot\n    #   'mqe'    : float    \u2014 global mean quantization error over all data\n    #   'W_mqe'  : ndarray (M,) \u2014 per-prototype MQE, nan for empty RFs\n    #   'delBMU' : float    \u2014 proportion of data whose BMU changed since\n    #                         the previous epoch; 1.0 at age=0 by definition\n    #   'fig'    : Figure or None \u2014 plot captured this epoch (or None)\n    self.learn_history_ = []\n</code></pre>"},{"location":"api/#gtsom.GTSOM.M","title":"M  <code>property</code>","text":"<pre><code>M\n</code></pre> <p>Number of prototypes (neurons).</p>"},{"location":"api/#gtsom.GTSOM.d","title":"d  <code>property</code>","text":"<pre><code>d\n</code></pre> <p>Dimensionality of the high-dimensional feature space.</p>"},{"location":"api/#gtsom.GTSOM.from_grid","title":"from_grid  <code>classmethod</code>","text":"<pre><code>from_grid(\n    X,\n    shape,\n    coord_init=\"hex\",\n    W_init=\"random\",\n    h_min=0.01,\n    random_state=None,\n    labels=None,\n)\n</code></pre> <p>Initialise a GTSOM on a regular grid output topology.</p> <p>Parameters:</p> Name Type Description Default <code>X</code> <code>(array - like, shape(N, d))</code> <p>Training data. Used for prototype initialisation; not stored.</p> required <code>shape</code> <code>tuple of int</code> <p>Grid dimensions, e.g. <code>(10, 10)</code> or <code>(5, 10, 4)</code>. The length of the tuple sets the embedding dimension (2 or 3).</p> required <code>coord_init</code> <code>(hex, rect)</code> <p>Grid layout. <code>'hex'</code> uses a hexagonal arrangement (Delaunay); <code>'rect'</code> uses an 8-connected rectangular grid.</p> <code>'hex'</code> <code>W_init</code> <code>(random, pca)</code> <p>How to initialise prototype vectors W.</p> <p><code>'random'</code>     Random sample of M rows from X. <code>'pca'</code>     Back-project grid neuron coordinates through the PCA of X,     placing each prototype at the corresponding point in high-d     feature space.</p> <code>'random'</code> <code>h_min</code> <code>float</code> <p>Neighbourhood kernel activation threshold; weights below this value are zeroed. Passed to :class:<code>NeighborKernel</code>.</p> <code>0.01</code> <code>random_state</code> <code>int or None</code> <p>Seed for reproducibility.</p> <code>None</code> <code>labels</code> <code>(array - like, shape(N))</code> <p>Optional observation labels. Any hashable type is accepted (int, str, float, etc.). If provided, prototype-level label summaries (WL, WL_Dist, WL_Purity) are computed during the initial recall and stored on <code>self.recaller</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>GTSOM</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>shape</code> is not a 2- or 3-tuple, <code>coord_init</code> or <code>W_init</code> are not recognised, or the grid has more neurons than data points.</p> Source code in <code>src/gtsom/gtsom.py</code> <pre><code>@classmethod\ndef from_grid(\n    cls,\n    X,\n    shape,\n    coord_init='hex',\n    W_init='random',\n    h_min=0.01,\n    random_state=None,\n    labels=None,\n):\n    \"\"\"\n    Initialise a GTSOM on a regular grid output topology.\n\n    Parameters\n    ----------\n    X : array-like, shape (N, d)\n        Training data. Used for prototype initialisation; not stored.\n    shape : tuple of int\n        Grid dimensions, e.g. ``(10, 10)`` or ``(5, 10, 4)``. The length\n        of the tuple sets the embedding dimension (2 or 3).\n    coord_init : {'hex', 'rect'}, default 'hex'\n        Grid layout. ``'hex'`` uses a hexagonal arrangement (Delaunay);\n        ``'rect'`` uses an 8-connected rectangular grid.\n    W_init : {'random', 'pca'}, default 'random'\n        How to initialise prototype vectors W.\n\n        ``'random'``\n            Random sample of M rows from X.\n        ``'pca'``\n            Back-project grid neuron coordinates through the PCA of X,\n            placing each prototype at the corresponding point in high-d\n            feature space.\n    h_min : float, default 0.01\n        Neighbourhood kernel activation threshold; weights below this\n        value are zeroed. Passed to :class:`NeighborKernel`.\n    random_state : int or None, default None\n        Seed for reproducibility.\n    labels : array-like, shape (N,), default None\n        Optional observation labels. Any hashable type is accepted\n        (int, str, float, etc.). If provided, prototype-level label\n        summaries (WL, WL_Dist, WL_Purity) are computed during the\n        initial recall and stored on ``self.recaller``.\n\n    Returns\n    -------\n    GTSOM\n\n    Raises\n    ------\n    ValueError\n        If ``shape`` is not a 2- or 3-tuple, ``coord_init`` or\n        ``W_init`` are not recognised, or the grid has more neurons\n        than data points.\n    \"\"\"\n    validator = DataValidator(X)\n    X = np.asarray(X)\n    rng = np.random.default_rng(random_state)\n\n    if coord_init not in ('hex', 'rect'):\n        raise ValueError(\n            f\"coord_init must be 'hex' or 'rect' for from_grid, \"\n            f\"got {coord_init!r}\"\n        )\n    if W_init not in ('random', 'pca'):\n        raise ValueError(\n            f\"W_init must be 'random' or 'pca' for from_grid, \"\n            f\"got {W_init!r}\"\n        )\n    if len(shape) not in (2, 3):\n        raise ValueError(\n            f\"shape must be a 2- or 3-tuple, got length {len(shape)}\"\n        )\n\n    # Build embedding \u2014 grid kind matches coord_init\n    embed = Embedding.from_grid(shape, kind=coord_init)\n    M = embed.coords.shape[0]\n\n    if M &gt; X.shape[0]:\n        raise ValueError(\n            f\"Grid has {M} neurons but X has only {X.shape[0]} rows. \"\n            f\"Reduce shape or provide more data.\"\n        )\n\n    # Initialise W\n    if W_init == 'random':\n        W = _init_W_random(X, M, rng)\n    else:  # 'pca'\n        W = _init_W_pca(X, embed, random_state)\n\n    recaller = VQRecaller(p=2, max_bmu=2, verbose=False)\n    kernel = NeighborKernel(h_min=h_min)\n\n    instance = cls(W, embed, recaller, kernel, random_state=random_state)\n    instance._validator = validator\n    instance.prevBMU = np.full(validator.shape[0], -1, dtype=int)\n    instance.W0 = W.copy()\n    instance.coords0 = embed.coords.copy()\n    instance._recall(X, labels=labels)\n    instance._snapshot(include_fig=True)\n    return instance\n</code></pre>"},{"location":"api/#gtsom.GTSOM.from_data","title":"from_data  <code>classmethod</code>","text":"<pre><code>from_data(\n    X,\n    M,\n    coord_dim=2,\n    coord_init=\"pca\",\n    W_init=\"kmeans\",\n    h_min=0.01,\n    random_state=None,\n    labels=None,\n)\n</code></pre> <p>Initialise a GTSOM with a data-driven output topology.</p> <p>Prototype vectors are found via vector quantisation of X, projected to <code>coord_dim</code> dimensions to define neuron positions, and a Delaunay triangulation of those positions defines the output topology.</p> <p>Parameters:</p> Name Type Description Default <code>X</code> <code>(array - like, shape(N, d))</code> <p>Training data.</p> required <code>M</code> <code>int</code> <p>Number of prototypes (neurons). Must be &gt;= 3 (required for Delaunay triangulation) and &lt;= N.</p> required <code>coord_dim</code> <code>(2, 3)</code> <p>Dimensionality of the output embedding.</p> <code>2</code> <code>coord_init</code> <code>(pca, le)</code> <p>Dimensionality reduction method for computing neuron coordinates from the initial prototype vectors.</p> <p><code>'pca'</code>     Randomised PCA (fast, linear). <code>'le'</code>     Laplacian Eigenmaps via sklearn SpectralEmbedding     (nonlinear, slower).</p> <code>'pca'</code> <code>W_init</code> <code>(kmeans, random)</code> <p>How to find initial prototype vectors.</p> <p><code>'kmeans'</code>     FAISS k-means via VQFitter (requires vqlp). <code>'random'</code>     Random sample of M rows from X.</p> <code>'kmeans'</code> <code>h_min</code> <code>float</code> <p>Neighbourhood kernel activation threshold; weights below this value are zeroed. Passed to :class:<code>NeighborKernel</code>.</p> <code>0.01</code> <code>random_state</code> <code>int or None</code> <p>Seed for reproducibility. Passed to VQ, dim-reduction, and any random sampling steps.</p> <code>None</code> <code>labels</code> <code>(array - like, shape(N))</code> <p>Optional observation labels. Any hashable type is accepted (int, str, float, etc.). If provided, prototype-level label summaries (WL, WL_Dist, WL_Purity) are computed during the initial recall and stored on <code>self.recaller</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>GTSOM</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>coord_dim</code> is not 2 or 3, <code>coord_init</code> or <code>W_init</code> are not recognised, or M is out of range.</p> <code>ImportError</code> <p>If <code>W_init='kmeans'</code> and vqlp is not installed.</p> Source code in <code>src/gtsom/gtsom.py</code> <pre><code>@classmethod\ndef from_data(\n    cls,\n    X,\n    M,\n    coord_dim=2,\n    coord_init='pca',\n    W_init='kmeans',\n    h_min=0.01,\n    random_state=None,\n    labels=None,\n):\n    \"\"\"\n    Initialise a GTSOM with a data-driven output topology.\n\n    Prototype vectors are found via vector quantisation of X, projected\n    to ``coord_dim`` dimensions to define neuron positions, and a Delaunay\n    triangulation of those positions defines the output topology.\n\n    Parameters\n    ----------\n    X : array-like, shape (N, d)\n        Training data.\n    M : int\n        Number of prototypes (neurons). Must be &gt;= 3 (required for\n        Delaunay triangulation) and &lt;= N.\n    coord_dim : {2, 3}, default 2\n        Dimensionality of the output embedding.\n    coord_init : {'pca', 'le'}, default 'pca'\n        Dimensionality reduction method for computing neuron coordinates\n        from the initial prototype vectors.\n\n        ``'pca'``\n            Randomised PCA (fast, linear).\n        ``'le'``\n            Laplacian Eigenmaps via sklearn SpectralEmbedding\n            (nonlinear, slower).\n    W_init : {'kmeans', 'random'}, default 'kmeans'\n        How to find initial prototype vectors.\n\n        ``'kmeans'``\n            FAISS k-means via VQFitter (requires vqlp).\n        ``'random'``\n            Random sample of M rows from X.\n    h_min : float, default 0.01\n        Neighbourhood kernel activation threshold; weights below this\n        value are zeroed. Passed to :class:`NeighborKernel`.\n    random_state : int or None, default None\n        Seed for reproducibility. Passed to VQ, dim-reduction, and\n        any random sampling steps.\n    labels : array-like, shape (N,), default None\n        Optional observation labels. Any hashable type is accepted\n        (int, str, float, etc.). If provided, prototype-level label\n        summaries (WL, WL_Dist, WL_Purity) are computed during the\n        initial recall and stored on ``self.recaller``.\n\n    Returns\n    -------\n    GTSOM\n\n    Raises\n    ------\n    ValueError\n        If ``coord_dim`` is not 2 or 3, ``coord_init`` or ``W_init``\n        are not recognised, or M is out of range.\n    ImportError\n        If ``W_init='kmeans'`` and vqlp is not installed.\n    \"\"\"\n    validator = DataValidator(X)\n    X = np.asarray(X)\n    rng = np.random.default_rng(random_state)\n\n    if coord_dim not in (2, 3):\n        raise ValueError(f\"coord_dim must be 2 or 3, got {coord_dim!r}\")\n    if coord_init not in ('pca', 'le'):\n        raise ValueError(\n            f\"coord_init must be 'pca' or 'le' for from_data, \"\n            f\"got {coord_init!r}\"\n        )\n    if W_init not in ('kmeans', 'random'):\n        raise ValueError(\n            f\"W_init must be 'kmeans' or 'random' for from_data, \"\n            f\"got {W_init!r}\"\n        )\n    if M &gt; X.shape[0]:\n        raise ValueError(\n            f\"M={M} exceeds number of data points N={X.shape[0]}.\"\n        )\n    if M &lt; 3:\n        raise ValueError(\n            f\"M must be at least 3 for Delaunay triangulation, got {M}.\"\n        )\n\n    # Step 1: initialise prototype vectors in high-d\n    if W_init == 'kmeans':\n        W = _init_W_kmeans(X, M, random_state)\n    else:  # 'random'\n        W = _init_W_random(X, M, rng)\n\n    # Step 2: project W to coord_dim to define neuron positions\n    if coord_init == 'pca':\n        coords = reduce_coords_pca(W, coord_dim, random_state)\n    else:  # 'le'\n        coords = reduce_coords_le(W, coord_dim, random_state)\n\n    # Step 3: build embedding via Delaunay triangulation of those coords\n    embed = Embedding.from_delaunay(coords)\n\n    # Step 4: build recaller and kernel\n    recaller = VQRecaller(p=2, max_bmu=2, verbose=False)\n    kernel = NeighborKernel(h_min=h_min)\n\n    instance = cls(W, embed, recaller, kernel, random_state=random_state)\n    instance._validator = validator\n    instance.prevBMU = np.full(validator.shape[0], -1, dtype=int)\n    instance.W0 = W.copy()\n    instance.coords0 = embed.coords.copy()\n    instance._recall(X, labels=labels)\n    instance._snapshot(include_fig=True)\n    return instance\n</code></pre>"},{"location":"api/#gtsom.GTSOM.compile","title":"compile","text":"<pre><code>compile(\n    rho_0,\n    rho_f,\n    rho_tau=None,\n    target_epochs=None,\n    anneal=\"exponential\",\n    n_jobs=None,\n)\n</code></pre> <p>Set the neighbourhood bandwidth annealing schedule.</p> <p>Must be called at least once before :meth:<code>fit</code>. Can be called again at any time to change the schedule \u2014 for example, to slow the decay rate for a refinement phase \u2014 without affecting <code>self.age</code>, <code>self.W</code>, or <code>self.learn_history_</code>.</p> <p>To start learning from scratch, construct a new instance via :meth:<code>from_grid</code> or :meth:<code>from_data</code> rather than calling <code>compile()</code> again.</p> <p>Exactly one of <code>rho_tau</code> or <code>target_epochs</code> must be supplied.</p> <p>Parameters:</p> Name Type Description Default <code>rho_0</code> <code>float</code> <p>Initial neighbourhood bandwidth, evaluated at the current <code>self.age</code> on the first subsequent :meth:<code>fit</code> epoch.</p> required <code>rho_f</code> <code>float</code> <p>Final (minimum) neighbourhood bandwidth.</p> required <code>rho_tau</code> <code>float</code> <p>Exponential decay time constant in epochs. Mutually exclusive with <code>target_epochs</code>.</p> <code>None</code> <code>target_epochs</code> <code>float</code> <p>Convenience alternative to <code>rho_tau</code>. The number of epochs over which rho decays from <code>rho_0</code> to <code>rho_f</code>. Mutually exclusive with <code>rho_tau</code>.</p> <code>None</code> <code>anneal</code> <code>exponential</code> <p>Annealing schedule type. Currently only exponential decay is supported.</p> <code>'exponential'</code> <code>n_jobs</code> <code>int or None</code> <p>Number of parallel threads for prototype updates. <code>None</code> or <code>-1</code> uses all available CPU cores. <code>1</code> forces single-threaded execution regardless of whether numba is installed (useful for debugging or benchmarking). Values &gt; 1 are silently clamped to <code>os.cpu_count()</code>. Ignored with a warning if numba is not installed.</p> <code>None</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If neither or both of <code>rho_tau</code> and <code>target_epochs</code> are supplied, or if <code>anneal</code> is not recognised.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; som.compile(rho_0=5.0, rho_f=0.3, target_epochs=50)\n&gt;&gt;&gt; som.compile(rho_0=5.0, rho_f=0.3, target_epochs=50, n_jobs=4)\n</code></pre> Source code in <code>src/gtsom/gtsom.py</code> <pre><code>def compile(self, rho_0, rho_f, rho_tau=None, target_epochs=None,\n            anneal='exponential', n_jobs=None):\n    \"\"\"\n    Set the neighbourhood bandwidth annealing schedule.\n\n    Must be called at least once before :meth:`fit`. Can be called\n    again at any time to change the schedule \u2014 for example, to slow\n    the decay rate for a refinement phase \u2014 without affecting\n    ``self.age``, ``self.W``, or ``self.learn_history_``.\n\n    To start learning from scratch, construct a new instance via\n    :meth:`from_grid` or :meth:`from_data` rather than calling\n    ``compile()`` again.\n\n    Exactly one of ``rho_tau`` or ``target_epochs`` must be supplied.\n\n    Parameters\n    ----------\n    rho_0 : float\n        Initial neighbourhood bandwidth, evaluated at the current\n        ``self.age`` on the first subsequent :meth:`fit` epoch.\n    rho_f : float\n        Final (minimum) neighbourhood bandwidth.\n    rho_tau : float, optional\n        Exponential decay time constant in epochs. Mutually exclusive\n        with ``target_epochs``.\n    target_epochs : float, optional\n        Convenience alternative to ``rho_tau``. The number of epochs\n        over which rho decays from ``rho_0`` to ``rho_f``.\n        Mutually exclusive with ``rho_tau``.\n    anneal : {'exponential'}, default 'exponential'\n        Annealing schedule type. Currently only exponential decay\n        is supported.\n    n_jobs : int or None, default None\n        Number of parallel threads for prototype updates.\n        ``None`` or ``-1`` uses all available CPU cores.\n        ``1`` forces single-threaded execution regardless of whether\n        numba is installed (useful for debugging or benchmarking).\n        Values &gt; 1 are silently clamped to ``os.cpu_count()``.\n        Ignored with a warning if numba is not installed.\n\n    Raises\n    ------\n    ValueError\n        If neither or both of ``rho_tau`` and ``target_epochs`` are\n        supplied, or if ``anneal`` is not recognised.\n\n    Examples\n    --------\n    &gt;&gt;&gt; som.compile(rho_0=5.0, rho_f=0.3, target_epochs=50)\n    &gt;&gt;&gt; som.compile(rho_0=5.0, rho_f=0.3, target_epochs=50, n_jobs=4)\n    \"\"\"\n    if anneal not in ('exponential',):\n        raise ValueError(\n            f\"anneal must be 'exponential', got {anneal!r}. \"\n            f\"Additional schedules may be added in future.\"\n        )\n    if rho_tau is None and target_epochs is None:\n        raise ValueError(\n            \"Exactly one of rho_tau or target_epochs must be supplied.\"\n        )\n    if rho_tau is not None and target_epochs is not None:\n        raise ValueError(\n            \"Supply either rho_tau or target_epochs, not both.\"\n        )\n\n    if anneal == 'exponential':\n        if rho_tau is not None:\n            self.rho_schedule = ExponentialAnneal(\n                initial=rho_0, final=rho_f, tau=rho_tau\n            )\n        else:\n            self.rho_schedule = ExponentialAnneal.from_target(\n                initial=rho_0, final=rho_f, target_epochs=target_epochs\n            )\n\n    # Resolve and store thread count\n    if not PARALLEL and n_jobs is not None and n_jobs != 1:\n        import warnings\n        warnings.warn(\n            f\"n_jobs={n_jobs} ignored: numba is not installed. \"\n            \"Install numba for parallel prototype updates.\",\n            UserWarning, stacklevel=2,\n        )\n    self.n_jobs = resolve_n_jobs(n_jobs)\n</code></pre>"},{"location":"api/#gtsom.GTSOM.fit","title":"fit","text":"<pre><code>fit(X, n_epochs, labels=None, verbose=True, plot_every=5)\n</code></pre> <p>Fit the GTSOM to data X using batch SOM learning.</p> <p>:meth:<code>compile</code> must be called before :meth:<code>fit</code>. The rho annealing schedule is owned by <code>self.rho_schedule</code> and advances based on <code>self.age</code>, so successive calls to <code>fit()</code> continue from where the previous call left off.</p> <p>Parameters:</p> Name Type Description Default <code>X</code> <code>(array - like, shape(N, d))</code> <p>Training data. Should be the same dataset passed to the classmethod used to construct this instance.</p> required <code>n_epochs</code> <code>int</code> <p>Number of training epochs to run in this call.</p> required <code>labels</code> <code>(array - like, shape(N))</code> <p>Optional observation labels. If provided, prototype-level label summaries (WL, WL_Dist, WL_Purity) are recomputed each epoch and stored on <code>self.recaller</code>.</p> <code>None</code> <code>verbose</code> <code>bool</code> <p>If True, print a formatted table of training metrics after each epoch. The table header is printed at the start of each <code>fit()</code> call and the separator is reprinted every 10 epochs for readability in long terminal runs. VQRecaller output is always suppressed regardless of this flag.</p> <code>True</code> <code>plot_every</code> <code>int</code> <p>Capture a SOM plot into <code>self.learn_history_</code> every this many epochs. Set to 0 to disable in-training plotting entirely. Plots are Figure objects stored in memory; nothing is displayed unless the caller explicitly calls <code>plt.show()</code> or <code>fig.savefig()</code>.</p> <code>5</code> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If :meth:<code>compile</code> has not been called.</p> Notes <p>GTSOM does not perform any internal scaling of X. It is the caller's responsibility to whiten, standardise, or otherwise preprocess X before calling fit.</p> Source code in <code>src/gtsom/gtsom.py</code> <pre><code>def fit(self, X, n_epochs, labels=None, verbose=True, plot_every=5):\n    \"\"\"\n    Fit the GTSOM to data X using batch SOM learning.\n\n    :meth:`compile` must be called before :meth:`fit`. The rho\n    annealing schedule is owned by ``self.rho_schedule`` and\n    advances based on ``self.age``, so successive calls to\n    ``fit()`` continue from where the previous call left off.\n\n    Parameters\n    ----------\n    X : array-like, shape (N, d)\n        Training data. Should be the same dataset passed to the\n        classmethod used to construct this instance.\n    n_epochs : int\n        Number of training epochs to run in this call.\n    labels : array-like, shape (N,), default None\n        Optional observation labels. If provided, prototype-level\n        label summaries (WL, WL_Dist, WL_Purity) are recomputed\n        each epoch and stored on ``self.recaller``.\n    verbose : bool, default True\n        If True, print a formatted table of training metrics after\n        each epoch. The table header is printed at the start of each\n        ``fit()`` call and the separator is reprinted every 10 epochs\n        for readability in long terminal runs. VQRecaller output is\n        always suppressed regardless of this flag.\n    plot_every : int, default 5\n        Capture a SOM plot into ``self.learn_history_`` every this many\n        epochs. Set to 0 to disable in-training plotting entirely.\n        Plots are Figure objects stored in memory; nothing is displayed\n        unless the caller explicitly calls ``plt.show()`` or\n        ``fig.savefig()``.\n\n    Raises\n    ------\n    RuntimeError\n        If :meth:`compile` has not been called.\n\n    Notes\n    -----\n    GTSOM does not perform any internal scaling of X. It is the\n    caller's responsibility to whiten, standardise, or otherwise\n    preprocess X before calling fit.\n    \"\"\"\n    if self.rho_schedule is None:\n        raise RuntimeError(\n            \"compile() must be called before fit(). \"\n            \"Call som.compile(rho_0=..., rho_f=..., target_epochs=...) first.\"\n        )\n\n    X = np.asarray(X)\n    if self._validator is not None:\n        self._validator.check(X, context=\"fit\")\n\n    # Column widths are fixed; epoch width scales with total epochs run\n    _HDR  = f\"{'Epoch':&gt;7}   {'rho':&gt;6}   {'MQE':&gt;8}   {'delBMU':&gt;7}\"\n    _SEP  = f\"{'-----':&gt;7}   {'------':&gt;6}   {'--------':&gt;8}   {'-------':&gt;7}\"\n\n    if verbose:\n        print(_HDR)\n        print(_SEP)\n\n    # Apply thread count for this fit() call; restore afterward\n    if PARALLEL:\n        from numba import set_num_threads, get_num_threads\n        _prev_threads = get_num_threads()\n        set_num_threads(self.n_jobs)\n\n    _t0 = time.perf_counter()\n    for local_epoch in range(n_epochs):\n        rho = self.rho_schedule(self.age)\n        self._compute_neighborhood(rho)\n        self._update_prototypes(X)\n        self._recall(X, labels=labels)\n        self.age += 1\n        include_fig = plot_every &gt; 0 and (self.age % plot_every == 0)\n        self._snapshot(include_fig=include_fig)\n        if verbose:\n            snap = self.learn_history_[-1]\n            print(\n                f\"{self.age:&gt;7d}   {rho:&gt;6.4f}   \"\n                f\"{snap['mqe']:&gt;8.4f}   {snap['delBMU']:&gt;7.4f}\"\n            )\n            # Reprint separator every 10 rows for long-run readability\n            if (local_epoch + 1) % 10 == 0:\n                print(_SEP)\n\n    if verbose:\n        print(_SEP)\n\n    self.train_time += time.perf_counter() - _t0\n\n    # Restore previous thread count\n    if PARALLEL:\n        set_num_threads(_prev_threads)\n</code></pre>"},{"location":"api/#gtsom.GTSOM.transform","title":"transform","text":"<pre><code>transform(X)\n</code></pre> <p>Map data points to output coordinates via BMU lookup.</p> <p>Parameters:</p> Name Type Description Default <code>X</code> <code>(array - like, shape(N, d))</code> required <p>Returns:</p> Name Type Description <code>coords</code> <code>(ndarray, shape(N, dim))</code> <p>Output coordinates of the BMU for each data point.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If the model has not been fitted yet.</p> Source code in <code>src/gtsom/gtsom.py</code> <pre><code>def transform(self, X):\n    \"\"\"\n    Map data points to output coordinates via BMU lookup.\n\n    Parameters\n    ----------\n    X : array-like, shape (N, d)\n\n    Returns\n    -------\n    coords : np.ndarray, shape (N, embed.dim)\n        Output coordinates of the BMU for each data point.\n\n    Raises\n    ------\n    RuntimeError\n        If the model has not been fitted yet.\n    \"\"\"\n    if self.age == 0:\n        raise RuntimeError(\n            \"transform() called before fit(). \"\n            \"Call fit() first to train the model.\"\n        )\n    X = np.asarray(X)\n    # Recall against current W to get fresh BMU assignments for X\n    self.recaller.recall(X=X, W=self.W)\n    return self.embed.coords[self.recaller.BMU[:, 0]]\n</code></pre>"},{"location":"api/#gtsom.GTSOM.fit_transform","title":"fit_transform","text":"<pre><code>fit_transform(X, **fit_kwargs)\n</code></pre> <p>Fit the GTSOM and return output coordinates.</p> <p>Equivalent to calling :meth:<code>fit</code> followed by :meth:<code>transform</code>.</p> <p>Parameters:</p> Name Type Description Default <code>X</code> <code>(array - like, shape(N, d))</code> required <code>**fit_kwargs</code> <p>Passed to :meth:<code>fit</code>.</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>coords</code> <code>(ndarray, shape(N, dim))</code> Source code in <code>src/gtsom/gtsom.py</code> <pre><code>def fit_transform(self, X, **fit_kwargs):\n    \"\"\"\n    Fit the GTSOM and return output coordinates.\n\n    Equivalent to calling :meth:`fit` followed by :meth:`transform`.\n\n    Parameters\n    ----------\n    X : array-like, shape (N, d)\n    **fit_kwargs\n        Passed to :meth:`fit`.\n\n    Returns\n    -------\n    coords : np.ndarray, shape (N, embed.dim)\n    \"\"\"\n    self.fit(X, **fit_kwargs)\n    return self.transform(X)\n</code></pre>"},{"location":"api/#gtsom.GTSOM.plot","title":"plot","text":"<pre><code>plot(\n    color_by=\"mqe\",\n    cmap_continuous=\"viridis\",\n    cmap_categorical=\"tab10\",\n    ax=None,\n    title=\"SOM Learning\",\n    subtitle=None,\n    xlabel=\"SOM$_1$\",\n    ylabel=\"SOM$_2$\",\n)\n</code></pre> <p>Plot the SOM in output (lattice) space.</p> <p>Neurons are drawn at their <code>embed.coords</code> positions, connected by edges from <code>embed.adjacency</code>, and coloured according to <code>color_by</code>. Nothing is displayed automatically \u2014 the returned Figure must be explicitly shown (<code>plt.show()</code>) or saved (<code>fig.savefig()</code>).</p> <p>Parameters:</p> Name Type Description Default <code>color_by</code> <code>(mqe, rfsize, labels)</code> <p>Quantity used to colour neurons.</p> <p><code>'mqe'</code>     Per-prototype mean quantization error from <code>learn_history_</code>.     Always available after any recall. Continuous colormap. <code>'rfsize'</code>     Receptive field size (<code>recaller.RFSize</code>). Always     available after any recall. Continuous colormap. <code>'labels'</code>     Winning label per prototype (<code>recaller.WL</code>). Requires     that labels were passed to the constructor or to     <code>fit()</code>. Categorical colormap. Raises <code>ValueError</code>     if <code>recaller.WL</code> is None.</p> <code>'mqe'</code> <code>cmap_continuous</code> <code>str or Colormap</code> <p>Matplotlib colormap for continuous quantities ('mqe', 'rfsize').</p> <code>'viridis'</code> <code>cmap_categorical</code> <code>str or Colormap</code> <p>Matplotlib colormap for categorical quantities ('labels').</p> <code>'tab10'</code> <code>ax</code> <code>Axes or None</code> <p>Axes to draw into. If None, a new Figure and Axes are created internally.</p> <code>None</code> <code>title</code> <code>str or None</code> <p>Main title line. Pass None to suppress.</p> <code>'SOM Learning'</code> <code>subtitle</code> <code>str or None</code> <p>Second title line. If None, auto-generates <code>f'Epoch = {self.age}'</code>.</p> <code>None</code> <code>xlabel</code> <code>str</code> <p>Label for the horizontal axis.</p> <code>'SOM_1'</code> <code>ylabel</code> <code>str</code> <p>Label for the vertical axis.</p> <code>'SOM_2'</code> <p>Returns:</p> Name Type Description <code>fig</code> <code>Figure</code> <p>Raises:</p> Type Description <code>NotImplementedError</code> <p>If <code>embed.dim != 2</code>.</p> <code>ValueError</code> <p>If <code>color_by='labels'</code> but no labels have been provided.</p> Source code in <code>src/gtsom/gtsom.py</code> <pre><code>def plot(\n    self,\n    color_by='mqe',\n    cmap_continuous='viridis',\n    cmap_categorical='tab10',\n    ax=None,\n    title='SOM Learning',\n    subtitle=None,\n    xlabel=r'SOM$_1$',\n    ylabel=r'SOM$_2$',\n):\n    \"\"\"\n    Plot the SOM in output (lattice) space.\n\n    Neurons are drawn at their ``embed.coords`` positions, connected\n    by edges from ``embed.adjacency``, and coloured according to\n    ``color_by``. Nothing is displayed automatically \u2014 the returned\n    Figure must be explicitly shown (``plt.show()``) or saved\n    (``fig.savefig()``).\n\n    Parameters\n    ----------\n    color_by : {'mqe', 'rfsize', 'labels'}, default 'mqe'\n        Quantity used to colour neurons.\n\n        ``'mqe'``\n            Per-prototype mean quantization error from ``learn_history_``.\n            Always available after any recall. Continuous colormap.\n        ``'rfsize'``\n            Receptive field size (``recaller.RFSize``). Always\n            available after any recall. Continuous colormap.\n        ``'labels'``\n            Winning label per prototype (``recaller.WL``). Requires\n            that labels were passed to the constructor or to\n            ``fit()``. Categorical colormap. Raises ``ValueError``\n            if ``recaller.WL`` is None.\n    cmap_continuous : str or Colormap, default 'viridis'\n        Matplotlib colormap for continuous quantities ('mqe',\n        'rfsize').\n    cmap_categorical : str or Colormap, default 'tab10'\n        Matplotlib colormap for categorical quantities ('labels').\n    ax : matplotlib.axes.Axes or None, default None\n        Axes to draw into. If None, a new Figure and Axes are\n        created internally.\n    title : str or None, default 'SOM Learning'\n        Main title line. Pass None to suppress.\n    subtitle : str or None, default None\n        Second title line. If None, auto-generates\n        ``f'Epoch = {self.age}'``.\n    xlabel : str, default 'SOM_1'\n        Label for the horizontal axis.\n    ylabel : str, default 'SOM_2'\n        Label for the vertical axis.\n\n    Returns\n    -------\n    fig : matplotlib.figure.Figure\n\n    Raises\n    ------\n    NotImplementedError\n        If ``embed.dim != 2``.\n    ValueError\n        If ``color_by='labels'`` but no labels have been provided.\n    \"\"\"\n    import matplotlib.pyplot as plt\n    import matplotlib.colors as mcolors\n    from matplotlib.patches import Patch\n\n    if self.embed.dim != 2:\n        raise NotImplementedError(\n            f\"plot() only supports 2-D embeddings; \"\n            f\"embed.dim={self.embed.dim}.\"\n        )\n\n    if color_by not in ('mqe', 'rfsize', 'labels'):\n        raise ValueError(\n            f\"color_by must be 'mqe', 'rfsize', or 'labels', \"\n            f\"got {color_by!r}.\"\n        )\n\n    if color_by == 'labels' and self.recaller.WL is None:\n        raise ValueError(\n            \"color_by='labels' requires labels to have been provided \"\n            \"at construction or during fit(). \"\n            \"recaller.WL is currently None.\"\n        )\n\n    # ------------------------------------------------------------------\n    # Resolve axes / figure\n    # ------------------------------------------------------------------\n    if ax is None:\n        fig, ax = plt.subplots(figsize=(7, 6))\n    else:\n        fig = ax.figure\n\n    # ------------------------------------------------------------------\n    # Draw adjacency edges (upper triangle only \u2014 matrix is symmetric)\n    # ------------------------------------------------------------------\n    cx = self.embed.adjacency.tocoo()\n    coords = self.embed.coords\n    for i, j in zip(cx.row, cx.col):\n        if i &lt; j:\n            ax.plot(\n                [coords[i, 0], coords[j, 0]],\n                [coords[i, 1], coords[j, 1]],\n                color='dimgrey', lw=0.8, alpha=0.5, zorder=1,\n            )\n\n    # ------------------------------------------------------------------\n    # Colour neurons\n    # ------------------------------------------------------------------\n    if color_by in ('mqe', 'rfsize'):\n        if color_by == 'mqe':\n            values = self.learn_history_[-1]['W_mqe']   # (M,), nan for empty RFs\n            cb_label = 'MQE'\n        else:\n            values = self.recaller.RFSize.astype(float)\n            cb_label = 'RF Size'\n\n        cmap = plt.get_cmap(cmap_continuous)\n        finite = np.isfinite(values)\n        vmin = float(np.nanmin(values)) if finite.any() else 0.0\n        vmax = float(np.nanmax(values)) if finite.any() else 1.0\n        norm = mcolors.Normalize(vmin=vmin, vmax=vmax)\n\n        # Finite-valued neurons: coloured by cmap\n        sc = ax.scatter(\n            coords[finite, 0], coords[finite, 1],\n            c=values[finite], cmap=cmap, norm=norm,\n            s=60, zorder=2, edgecolors='black', linewidths=0.4,\n        )\n        fig.colorbar(sc, ax=ax, label=cb_label, shrink=0.85)\n\n        # Empty-RF neurons (nan): grey\n        empty = ~finite\n        if empty.any():\n            ax.scatter(\n                coords[empty, 0], coords[empty, 1],\n                c='lightgrey', s=60, zorder=2,\n                edgecolors='black', linewidths=0.4,\n            )\n\n    else:  # color_by == 'labels'\n        WL = self.recaller.WL          # object array (M,), None for empty\n        unq_labels = self.recaller.WL_unq\n        n_labels = len(unq_labels)\n        cmap = plt.get_cmap(cmap_categorical, n_labels)\n        label_to_color = {lbl: cmap(i) for i, lbl in enumerate(unq_labels)}\n\n        node_colors = [\n            label_to_color[lbl] if lbl is not None else 'lightgrey'\n            for lbl in WL\n        ]\n        ax.scatter(\n            coords[:, 0], coords[:, 1],\n            c=node_colors, s=60, zorder=2,\n            edgecolors='black', linewidths=0.4,\n        )\n\n        # Legend\n        legend_elements = [\n            Patch(facecolor=label_to_color[lbl], edgecolor='black',\n                  label=str(lbl))\n            for lbl in unq_labels\n        ]\n        has_empty = any(lbl is None for lbl in WL)\n        if has_empty:\n            legend_elements.append(\n                Patch(facecolor='lightgrey', edgecolor='black',\n                      label='Empty RF')\n            )\n        ax.legend(handles=legend_elements, loc='best', fontsize=8,\n                  title='Label')\n\n    # ------------------------------------------------------------------\n    # Titles and axis labels\n    # ------------------------------------------------------------------\n    sub = subtitle if subtitle is not None else f'Epoch = {self.age}'\n    if title is not None:\n        ax.set_title(f'{title}\\n{sub}', fontsize=10)\n    else:\n        ax.set_title(sub, fontsize=10)\n\n    ax.set_xlabel(xlabel)\n    ax.set_ylabel(ylabel)\n    ax.set_aspect('equal', adjustable='datalim')\n\n    plt.tight_layout()\n    return fig\n</code></pre>"},{"location":"api/#gtsom.GTSOM.update_embedding","title":"update_embedding","text":"<pre><code>update_embedding(coords)\n</code></pre> <p>Replace the output-space embedding coordinates and refresh the neighbourhood weight matrix atomically.</p> <p>Calls <code>self.embed.update_coords(coords)</code> to validate the new positions, rebuild adjacency, and recompute geodesic distances. Then immediately recomputes <code>self.nbr_W</code> from the updated topology using the current annealing bandwidth <code>self.rho_schedule(self.age)</code>, so that the next :meth:<code>fit</code> call starts with a consistent neighbourhood structure.</p> <p>This method is the correct way to inject an externally computed coordinate update (e.g. from a tSNE or UMAP step) into a fitted GTSOM without leaving <code>nbr_W</code> stale.</p> <p>Parameters:</p> Name Type Description Default <code>coords</code> <code>(array - like, shape(M, dim))</code> <p>New low-dimensional prototype positions. Must match the current embedding shape <code>(M, dim)</code> exactly.</p> required <p>Returns:</p> Name Type Description <code>self</code> <code>GTSOM</code> <p>Returns self for method chaining.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If :meth:<code>compile</code> has not been called yet (<code>self.rho_schedule</code> is None). The neighbourhood weight matrix cannot be recomputed without a valid annealing schedule.</p> <code>ValueError</code> <p>Propagated from :meth:<code>Embedding.update_coords</code> if the shape of <code>coords</code> does not match the current embedding shape.</p> Source code in <code>src/gtsom/gtsom.py</code> <pre><code>def update_embedding(self, coords):\n    \"\"\"\n    Replace the output-space embedding coordinates and refresh the\n    neighbourhood weight matrix atomically.\n\n    Calls ``self.embed.update_coords(coords)`` to validate the new\n    positions, rebuild adjacency, and recompute geodesic distances.\n    Then immediately recomputes ``self.nbr_W`` from the updated\n    topology using the current annealing bandwidth\n    ``self.rho_schedule(self.age)``, so that the next :meth:`fit`\n    call starts with a consistent neighbourhood structure.\n\n    This method is the correct way to inject an externally computed\n    coordinate update (e.g. from a tSNE or UMAP step) into a fitted\n    GTSOM without leaving ``nbr_W`` stale.\n\n    Parameters\n    ----------\n    coords : array-like, shape (M, dim)\n        New low-dimensional prototype positions. Must match the\n        current embedding shape ``(M, dim)`` exactly.\n\n    Returns\n    -------\n    self : GTSOM\n        Returns self for method chaining.\n\n    Raises\n    ------\n    RuntimeError\n        If :meth:`compile` has not been called yet (``self.rho_schedule``\n        is None). The neighbourhood weight matrix cannot be recomputed\n        without a valid annealing schedule.\n    ValueError\n        Propagated from :meth:`Embedding.update_coords` if the shape\n        of ``coords`` does not match the current embedding shape.\n    \"\"\"\n    if self.rho_schedule is None:\n        raise RuntimeError(\n            \"compile() must be called before update_embedding(). \"\n            \"Call som.compile(rho_0=..., rho_f=..., target_epochs=...) \"\n            \"first so that a valid rho_schedule is available for \"\n            \"recomputing the neighbourhood weight matrix.\"\n        )\n    self.embed.update_coords(coords)\n    self._compute_neighborhood(self.rho_schedule(self.age))\n    return self\n</code></pre>"},{"location":"api/#utilities","title":"Utilities","text":""},{"location":"api/#gtsom.utils.ExponentialAnneal","title":"gtsom.utils.ExponentialAnneal","text":"<pre><code>ExponentialAnneal(initial, final, tau)\n</code></pre> <p>Exponential annealing schedule for a scalar parameter.</p> <p>Computes the annealed value at a given age (epoch count) as::</p> <pre><code>value(age) = initial * (final / initial) ** (age / tau)\n</code></pre> <p>The result is clipped at <code>final</code> for all <code>age &gt;= target_epochs</code>, ensuring the final value is respected exactly regardless of how many epochs have elapsed.</p> <p>Intended for use with any parameter that requires monotonic decay during training \u2014 neighbourhood bandwidth (rho), learning rate, etc.</p> <p>Parameters:</p> Name Type Description Default <code>initial</code> <code>float</code> <p>Starting value of the parameter (at age=0).</p> required <code>final</code> <code>float</code> <p>Minimum (floor) value of the parameter. The schedule is clipped at this value for all ages beyond <code>target_epochs</code>.</p> required <code>tau</code> <code>float</code> <p>Decay time constant in epochs. Smaller values decay faster. Computed internally from <code>target_epochs</code> when that convenience argument is used instead.</p> required <p>Examples:</p> <p>Direct construction with tau:</p> <pre><code>&gt;&gt;&gt; schedule = ExponentialAnneal(initial=5.0, final=0.3, tau=20)\n&gt;&gt;&gt; schedule(0)\n5.0\n&gt;&gt;&gt; schedule(20)   # one tau \u2014 falls to ~37% of the initial range\n0.549...\n&gt;&gt;&gt; schedule(1000)  # far beyond target \u2014 clipped at final\n0.3\n</code></pre> <p>Construction from a target epoch count:</p> <pre><code>&gt;&gt;&gt; schedule = ExponentialAnneal.from_target(initial=5.0, final=0.3,\n...                                           target_epochs=50)\n&gt;&gt;&gt; round(schedule(50), 6)\n0.3\n</code></pre> Source code in <code>src/gtsom/utils.py</code> <pre><code>def __init__(self, initial, final, tau):\n    if initial &lt;= 0:\n        raise ValueError(f\"initial must be positive, got {initial}\")\n    if final &lt;= 0:\n        raise ValueError(f\"final must be positive, got {final}\")\n    if final &gt;= initial:\n        raise ValueError(\n            f\"final ({final}) must be less than initial ({initial}) \"\n            f\"for a decay schedule.\"\n        )\n    if tau &lt;= 0:\n        raise ValueError(f\"tau must be positive, got {tau}\")\n\n    self.initial = float(initial)\n    self.final = float(final)\n    self.tau = float(tau)\n\n    # Precompute target_epochs: the age at which the raw exponential\n    # first reaches final (used for clipping and __repr__)\n    self.target_epochs = -self.tau * np.log(self.final / self.initial)\n</code></pre>"},{"location":"api/#gtsom.utils.ExponentialAnneal.from_target","title":"from_target  <code>classmethod</code>","text":"<pre><code>from_target(initial, final, target_epochs)\n</code></pre> <p>Construct a schedule where <code>target_epochs</code> is the half-life.</p> <p><code>target_epochs</code> is the number of epochs at which rho reaches the geometric midpoint between <code>initial</code> and <code>final</code> on a log scale. The schedule continues decaying beyond this point and is clipped at <code>final</code> at <code>2 * target_epochs</code>.</p> <p>This gives an intuitive interpretation: the parameter is still actively decaying throughout the first <code>target_epochs</code> epochs, and has fully settled by <code>2 * target_epochs</code>.</p> <p>Parameters:</p> Name Type Description Default <code>initial</code> <code>float</code> <p>Starting value at age=0.</p> required <code>final</code> <code>float</code> <p>Floor value; clipped at this value from age <code>2 * target_epochs</code> onward.</p> required <code>target_epochs</code> <code>float</code> <p>Half-life in epochs \u2014 the age at which the parameter reaches the geometric midpoint <code>sqrt(initial * final)</code>.</p> required <p>Returns:</p> Type Description <code>ExponentialAnneal</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; s = ExponentialAnneal.from_target(initial=3.0, final=0.3,\n...                                    target_epochs=50)\n&gt;&gt;&gt; round(s(0), 4)\n3.0\n&gt;&gt;&gt; round(s(50), 4)   # geometric midpoint of 3.0 and 0.3\n0.9487\n&gt;&gt;&gt; round(s(100), 4)  # clipped at final\n0.3\n</code></pre> Source code in <code>src/gtsom/utils.py</code> <pre><code>@classmethod\ndef from_target(cls, initial, final, target_epochs):\n    \"\"\"\n    Construct a schedule where ``target_epochs`` is the half-life.\n\n    ``target_epochs`` is the number of epochs at which rho reaches\n    the geometric midpoint between ``initial`` and ``final`` on a\n    log scale. The schedule continues decaying beyond this point\n    and is clipped at ``final`` at ``2 * target_epochs``.\n\n    This gives an intuitive interpretation: the parameter is still\n    actively decaying throughout the first ``target_epochs`` epochs,\n    and has fully settled by ``2 * target_epochs``.\n\n    Parameters\n    ----------\n    initial : float\n        Starting value at age=0.\n    final : float\n        Floor value; clipped at this value from age ``2 *\n        target_epochs`` onward.\n    target_epochs : float\n        Half-life in epochs \u2014 the age at which the parameter reaches\n        the geometric midpoint ``sqrt(initial * final)``.\n\n    Returns\n    -------\n    ExponentialAnneal\n\n    Examples\n    --------\n    &gt;&gt;&gt; s = ExponentialAnneal.from_target(initial=3.0, final=0.3,\n    ...                                    target_epochs=50)\n    &gt;&gt;&gt; round(s(0), 4)\n    3.0\n    &gt;&gt;&gt; round(s(50), 4)   # geometric midpoint of 3.0 and 0.3\n    0.9487\n    &gt;&gt;&gt; round(s(100), 4)  # clipped at final\n    0.3\n    \"\"\"\n    if target_epochs &lt;= 0:\n        raise ValueError(\n            f\"target_epochs must be positive, got {target_epochs}\"\n        )\n    # tau such that age=target_epochs gives the geometric midpoint:\n    # initial * (final/initial)^(target_epochs/tau) = sqrt(initial*final)\n    # =&gt; (final/initial)^(target_epochs/tau) = sqrt(final/initial)\n    # =&gt; target_epochs/tau = 0.5\n    # =&gt; tau = 2 * target_epochs\n    tau = 2.0 * target_epochs\n    return cls(initial=initial, final=final, tau=tau)\n</code></pre>"},{"location":"api/#gtsom.utils.ExponentialAnneal.__call__","title":"__call__","text":"<pre><code>__call__(age)\n</code></pre> <p>Return the annealed parameter value at the given age.</p> <p>The raw exponential is clipped at <code>final</code> for all ages beyond <code>target_epochs</code>, ensuring the floor is respected exactly.</p> <p>Parameters:</p> Name Type Description Default <code>age</code> <code>int or float</code> <p>Current epoch count (0-based).</p> required <p>Returns:</p> Type Description <code>float</code> Source code in <code>src/gtsom/utils.py</code> <pre><code>def __call__(self, age):\n    \"\"\"\n    Return the annealed parameter value at the given age.\n\n    The raw exponential is clipped at ``final`` for all ages beyond\n    ``target_epochs``, ensuring the floor is respected exactly.\n\n    Parameters\n    ----------\n    age : int or float\n        Current epoch count (0-based).\n\n    Returns\n    -------\n    float\n    \"\"\"\n    raw = self.initial * (self.final / self.initial) ** (age / self.tau)\n    return float(max(raw, self.final))\n</code></pre>"},{"location":"api/#gtsom.utils.ExponentialAnneal.values","title":"values","text":"<pre><code>values(n_epochs)\n</code></pre> <p>Return the annealed values for epochs 0 through n_epochs - 1.</p> <p>Useful for inspecting or plotting the planned schedule before training begins.</p> <p>Parameters:</p> Name Type Description Default <code>n_epochs</code> <code>int</code> required <p>Returns:</p> Type Description <code>(ndarray, shape(n_epochs))</code> Source code in <code>src/gtsom/utils.py</code> <pre><code>def values(self, n_epochs):\n    \"\"\"\n    Return the annealed values for epochs 0 through n_epochs - 1.\n\n    Useful for inspecting or plotting the planned schedule before\n    training begins.\n\n    Parameters\n    ----------\n    n_epochs : int\n\n    Returns\n    -------\n    np.ndarray, shape (n_epochs,)\n    \"\"\"\n    ages = np.arange(n_epochs, dtype=float)\n    raw = self.initial * (self.final / self.initial) ** (ages / self.tau)\n    return np.maximum(raw, self.final).astype(float)\n</code></pre>"},{"location":"api/#gtsom.utils.DataValidator","title":"gtsom.utils.DataValidator","text":"<pre><code>DataValidator(X)\n</code></pre> <p>Validates a data matrix X and stores a lightweight fingerprint for consistency checking across subsequent calls.</p> <p>Construction validates X (must be 2-D) and records its fingerprint. The :meth:<code>check</code> method warns if a later X does not match.</p> <p>Parameters:</p> Name Type Description Default <code>X</code> <code>array - like</code> <p>Data matrix to validate and fingerprint.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If X is not 2-D after conversion to ndarray.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; validator = DataValidator(np.ones((100, 5)))\n&gt;&gt;&gt; validator.shape\n(100, 5)\n&gt;&gt;&gt; validator.check(np.ones((100, 5)))   # silent \u2014 matches\n&gt;&gt;&gt; validator.check(np.ones((200, 5)), context=\"fit\")  # warns\n</code></pre> Source code in <code>src/gtsom/utils.py</code> <pre><code>def __init__(self, X):\n    X = np.asarray(X)\n    if X.ndim != 2:\n        raise ValueError(f\"X must be 2-D, got shape {X.shape}\")\n    self._fingerprint = (\n        X.shape,\n        X.dtype,\n        float(X.mean()),\n        float(X.std()),\n        float(X.min()),\n        float(X.max()),\n    )\n</code></pre>"},{"location":"api/#gtsom.utils.DataValidator.shape","title":"shape  <code>property</code>","text":"<pre><code>shape\n</code></pre> <p>Shape (N, d) of the validated dataset.</p>"},{"location":"api/#gtsom.utils.DataValidator.dtype","title":"dtype  <code>property</code>","text":"<pre><code>dtype\n</code></pre> <p>dtype of the validated dataset.</p>"},{"location":"api/#gtsom.utils.DataValidator.check","title":"check","text":"<pre><code>check(X, context='')\n</code></pre> <p>Warn if X does not match the stored fingerprint.</p> <p>Parameters:</p> Name Type Description Default <code>X</code> <code>array - like</code> <p>Incoming data matrix to check.</p> required <code>context</code> <code>str</code> <p>Caller name included in the warning message for traceability.</p> <code>''</code> Source code in <code>src/gtsom/utils.py</code> <pre><code>def check(self, X, context=\"\"):\n    \"\"\"\n    Warn if X does not match the stored fingerprint.\n\n    Parameters\n    ----------\n    X : array-like\n        Incoming data matrix to check.\n    context : str, optional\n        Caller name included in the warning message for traceability.\n    \"\"\"\n    X = np.asarray(X)\n    current = (\n        X.shape,\n        X.dtype,\n        float(X.mean()),\n        float(X.std()),\n        float(X.min()),\n        float(X.max()),\n    )\n    if current != self._fingerprint:\n        fp = self._fingerprint\n        prefix = f\"{context}: \" if context else \"\"\n        warnings.warn(\n            f\"{prefix}X does not match the dataset used at initialisation. \"\n            f\"Expected shape={fp[0]}, dtype={fp[1]}, \"\n            f\"mean={fp[2]:.4g}, std={fp[3]:.4g}, \"\n            f\"min={fp[4]:.4g}, max={fp[5]:.4g}. \"\n            f\"Got shape={current[0]}, dtype={current[1]}, \"\n            f\"mean={current[2]:.4g}, std={current[3]:.4g}, \"\n            f\"min={current[4]:.4g}, max={current[5]:.4g}.\",\n            UserWarning,\n            stacklevel=3,\n        )\n</code></pre>"},{"location":"api/#embedding","title":"Embedding","text":""},{"location":"api/#gtsom.embedding.Embedding","title":"gtsom.embedding.Embedding","text":"<pre><code>Embedding(\n    coords, adj_metric, dist_metric=\"geodesic\", _shape=None\n)\n</code></pre> <p>Low-dimensional embedding of SOM prototype positions, including graph topology and pairwise distances.</p> <p>Stores the output-space coordinates of M prototypes, a sparse adjacency matrix defining their topological connections, and a dense pairwise distance matrix derived from that adjacency.</p> <p>Attributes:</p> Name Type Description <code>coords</code> <code>(ndarray, shape(M, dim))</code> <p>Low-dimensional positions of the prototypes. Mutable \u2014 can be updated externally (e.g. by a tSNE/UMAP step), followed by a call to compute_topo() to rebuild adjacency and distances.</p> <code>dim</code> <code>int</code> <p>Dimensionality of the embedding space (2 or 3). Inferred from coords.</p> <code>adjacency</code> <code>(csr_matrix, shape(M, M))</code> <p>Sparse symmetric adjacency matrix. Entry (i, j) = 1 if prototypes i and j are graph neighbors, 0 otherwise. Populated by compute_topo().</p> <code>adj_metric</code> <code>str</code> <p>Algorithm used to construct the adjacency. One of: 'delaunay', 'gabriel', 'grid_rect', 'grid_hex'.</p> <code>dist</code> <code>np.ndarray, shape (M, M), dtype float32</code> <p>Dense pairwise distance matrix. Populated by compute_topo().</p> <code>dist_metric</code> <code>str</code> <p>Distance type used to populate dist. Currently supported: 'geodesic' (unweighted hop-count shortest path).</p> <code>M</code> <code>int(property)</code> <p>Number of prototypes.</p> <p>Base constructor. Stores coords and metric specs, then calls compute_topo() to build adjacency and dist.</p> <p>Prefer the classmethods (from_delaunay, from_grid) over calling this directly.</p> <p>Parameters:</p> Name Type Description Default <code>coords</code> <code>(array - like, shape(M, dim))</code> <p>Low-dimensional prototype positions. dim must be 2 or 3.</p> required <code>adj_metric</code> <code>str</code> <p>Algorithm to use for adjacency construction.</p> required <code>dist_metric</code> <code>str</code> <p>Distance type for computing dist.</p> <code>'geodesic'</code> <code>_shape</code> <code>tuple of int</code> <p>Grid shape \u2014 stored internally by from_grid() so that compute_topo() can reconstruct exact grid adjacency.</p> <code>None</code> Source code in <code>src/gtsom/embedding.py</code> <pre><code>def __init__(self, coords, adj_metric, dist_metric=\"geodesic\", _shape=None):\n    \"\"\"\n    Base constructor. Stores coords and metric specs, then calls\n    compute_topo() to build adjacency and dist.\n\n    Prefer the classmethods (from_delaunay, from_grid) over calling\n    this directly.\n\n    Parameters\n    ----------\n    coords : array-like, shape (M, dim)\n        Low-dimensional prototype positions. dim must be 2 or 3.\n    adj_metric : str\n        Algorithm to use for adjacency construction.\n    dist_metric : str, default 'geodesic'\n        Distance type for computing dist.\n    _shape : tuple of int, optional\n        Grid shape \u2014 stored internally by from_grid() so that\n        compute_topo() can reconstruct exact grid adjacency.\n    \"\"\"\n    self.coords = np.asarray(coords, dtype=float)\n    self.adj_metric = adj_metric\n    self.dist_metric = dist_metric\n    self._shape = _shape  # only set for grid embeddings\n    self.adjacency = None\n    self.dist = None\n    self._validate()\n    self.compute_topo()\n</code></pre>"},{"location":"api/#gtsom.embedding.Embedding.M","title":"M  <code>property</code>","text":"<pre><code>M\n</code></pre> <p>Number of prototypes.</p>"},{"location":"api/#gtsom.embedding.Embedding.dim","title":"dim  <code>property</code>","text":"<pre><code>dim\n</code></pre> <p>Dimensionality of the embedding space (2 or 3).</p>"},{"location":"api/#gtsom.embedding.Embedding.compute_topo","title":"compute_topo","text":"<pre><code>compute_topo()\n</code></pre> <p>Recompute adjacency from current coords (using adj_metric), then recompute dist (using dist_metric).</p> <p>Call this after updating coords externally (e.g. after a tSNE/UMAP coordinate update step). For grid-based embeddings this is called once at construction and rarely thereafter.</p> <p>Returns:</p> Name Type Description <code>self</code> <code>Embedding</code> <p>Returns self for method chaining.</p> Source code in <code>src/gtsom/embedding.py</code> <pre><code>def compute_topo(self):\n    \"\"\"\n    Recompute adjacency from current coords (using adj_metric), then\n    recompute dist (using dist_metric).\n\n    Call this after updating coords externally (e.g. after a tSNE/UMAP\n    coordinate update step). For grid-based embeddings this is called\n    once at construction and rarely thereafter.\n\n    Returns\n    -------\n    self : Embedding\n        Returns self for method chaining.\n    \"\"\"\n    if self.adj_metric in (\"delaunay\", \"grid_hex\"):\n        # Delaunay correctly recovers hex grid 6-connectivity and\n        # handles arbitrary/random coord layouts.\n        self.adjacency = _delaunay_adjacency(self.coords)\n    elif self.adj_metric == \"grid_rect\":\n        # Delaunay on a regular grid is ambiguous for square cells:\n        # it picks one diagonal per square, missing the other half.\n        # Use an explicit 8-connected grid adjacency instead.\n        if self._shape is None:\n            raise RuntimeError(\n                \"grid_rect adjacency requires _shape to be set. \"\n                \"Use Embedding.from_grid() to construct rect grid embeddings.\"\n            )\n        self.adjacency = _rect_grid_adjacency(self._shape)\n    elif self.adj_metric == \"gabriel\":\n        self.adjacency = _gabriel_adjacency(self.coords)\n\n    self.dist = self._compute_dist()\n    return self\n</code></pre>"},{"location":"api/#gtsom.embedding.Embedding.update_coords","title":"update_coords","text":"<pre><code>update_coords(coords)\n</code></pre> <p>Replace the embedding coordinates and atomically rebuild topology.</p> <p>Sets <code>self.coords</code> to the provided array and immediately calls <code>self.compute_topo()</code> to rebuild <code>self.adjacency</code> and <code>self.dist</code> from the new positions. Callers do not need to trigger <code>compute_topo()</code> manually after changing coords.</p> <p>Parameters:</p> Name Type Description Default <code>coords</code> <code>(array - like, shape(M, dim))</code> <p>New low-dimensional prototype positions. Must match the current shape <code>(M, dim)</code> exactly \u2014 neither the number of prototypes nor the embedding dimensionality may change.</p> required <p>Returns:</p> Name Type Description <code>self</code> <code>Embedding</code> <p>Returns self for method chaining.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the shape of <code>coords</code> does not match the current <code>self.coords</code> shape <code>(M, dim)</code>.</p> Source code in <code>src/gtsom/embedding.py</code> <pre><code>def update_coords(self, coords):\n    \"\"\"\n    Replace the embedding coordinates and atomically rebuild topology.\n\n    Sets ``self.coords`` to the provided array and immediately calls\n    ``self.compute_topo()`` to rebuild ``self.adjacency`` and\n    ``self.dist`` from the new positions. Callers do not need to\n    trigger ``compute_topo()`` manually after changing coords.\n\n    Parameters\n    ----------\n    coords : array-like, shape (M, dim)\n        New low-dimensional prototype positions. Must match the current\n        shape ``(M, dim)`` exactly \u2014 neither the number of prototypes\n        nor the embedding dimensionality may change.\n\n    Returns\n    -------\n    self : Embedding\n        Returns self for method chaining.\n\n    Raises\n    ------\n    ValueError\n        If the shape of ``coords`` does not match the current\n        ``self.coords`` shape ``(M, dim)``.\n    \"\"\"\n    coords = np.asarray(coords, dtype=float)\n    expected = self.coords.shape\n    if coords.shape != expected:\n        raise ValueError(\n            f\"coords shape mismatch: expected {expected}, got {coords.shape}. \"\n            f\"The number of prototypes (M={expected[0]}) and embedding \"\n            f\"dimensionality (dim={expected[1]}) must not change.\"\n        )\n    self.coords = coords\n    self.compute_topo()\n    return self\n</code></pre>"},{"location":"api/#gtsom.embedding.Embedding.from_delaunay","title":"from_delaunay  <code>classmethod</code>","text":"<pre><code>from_delaunay(coords, dist_metric='geodesic')\n</code></pre> <p>Construct an Embedding from the Delaunay triangulation of coords.</p> <p>Parameters:</p> Name Type Description Default <code>coords</code> <code>(array - like, shape(M, dim))</code> <p>Low-dimensional prototype positions. dim must be 2 or 3.</p> required <code>dist_metric</code> <code>str</code> <p>Distance type for computing dist.</p> <code>'geodesic'</code> <p>Returns:</p> Type Description <code>Embedding</code> Source code in <code>src/gtsom/embedding.py</code> <pre><code>@classmethod\ndef from_delaunay(cls, coords, dist_metric=\"geodesic\"):\n    \"\"\"\n    Construct an Embedding from the Delaunay triangulation of coords.\n\n    Parameters\n    ----------\n    coords : array-like, shape (M, dim)\n        Low-dimensional prototype positions. dim must be 2 or 3.\n    dist_metric : str, default 'geodesic'\n        Distance type for computing dist.\n\n    Returns\n    -------\n    Embedding\n    \"\"\"\n    return cls(coords, adj_metric=\"delaunay\", dist_metric=dist_metric)\n</code></pre>"},{"location":"api/#gtsom.embedding.Embedding.from_grid","title":"from_grid  <code>classmethod</code>","text":"<pre><code>from_grid(shape, kind='hex', dist_metric='geodesic')\n</code></pre> <p>Construct an Embedding from a regular grid.</p> <p>Coordinates are generated analytically from the grid shape and type. For hexagonal grids, adjacency is derived via Delaunay triangulation (which correctly recovers 6-connectivity in 2D and richer inter-layer connectivity in 3D). For rectangular grids, an explicit 8-connected adjacency is constructed directly to avoid the diagonal ambiguity of Delaunay on regular square grids.</p> <p>Parameters:</p> Name Type Description Default <code>shape</code> <code>tuple of int</code> <p>Grid dimensions. (nrows, ncols) for 2D grids, or (nrows, ncols, ndepth) for 3D grids.</p> required <code>kind</code> <code>str</code> <p>Grid type: 'hex' for hexagonal, 'rect' for rectangular.</p> <code>'hex'</code> <code>dist_metric</code> <code>str</code> <p>Distance type for computing dist.</p> <code>'geodesic'</code> <p>Returns:</p> Type Description <code>Embedding</code> Source code in <code>src/gtsom/embedding.py</code> <pre><code>@classmethod\ndef from_grid(cls, shape, kind=\"hex\", dist_metric=\"geodesic\"):\n    \"\"\"\n    Construct an Embedding from a regular grid.\n\n    Coordinates are generated analytically from the grid shape and\n    type. For hexagonal grids, adjacency is derived via Delaunay\n    triangulation (which correctly recovers 6-connectivity in 2D and\n    richer inter-layer connectivity in 3D). For rectangular grids,\n    an explicit 8-connected adjacency is constructed directly to\n    avoid the diagonal ambiguity of Delaunay on regular square grids.\n\n    Parameters\n    ----------\n    shape : tuple of int\n        Grid dimensions. (nrows, ncols) for 2D grids, or\n        (nrows, ncols, ndepth) for 3D grids.\n    kind : str, default 'hex'\n        Grid type: 'hex' for hexagonal, 'rect' for rectangular.\n    dist_metric : str, default 'geodesic'\n        Distance type for computing dist.\n\n    Returns\n    -------\n    Embedding\n    \"\"\"\n    if kind == \"hex\":\n        coords = _hex_grid_coords(shape)\n        return cls(\n            coords, adj_metric=\"grid_hex\",\n            dist_metric=dist_metric, _shape=shape,\n        )\n    elif kind == \"rect\":\n        coords = _rect_grid_coords(shape)\n        return cls(\n            coords, adj_metric=\"grid_rect\",\n            dist_metric=dist_metric, _shape=shape,\n        )\n    else:\n        raise ValueError(f\"kind must be 'hex' or 'rect', got '{kind}'\")\n</code></pre>"},{"location":"api/#neighborkernel","title":"NeighborKernel","text":""},{"location":"api/#gtsom.kernel.NeighborKernel","title":"gtsom.kernel.NeighborKernel","text":"<pre><code>NeighborKernel(kind='exp', h_min=0.01)\n</code></pre> <p>Computes sparse neighborhood activation weights for SOM prototype updates.</p> <p>At each learning step, the kernel takes the pairwise distance matrix between prototypes (from Embedding.dist) and a per-prototype bandwidth vector rho, and returns a sparse matrix of neighborhood influence weights.</p> <p>The kernel function is set at construction and applied uniformly via compute(). Per-prototype rho values allow spatially adaptive bandwidths, though in practice a single global rho (broadcast to length M) is typical.</p> <p>Parameters:</p> Name Type Description Default <code>kind</code> <code>str</code> <p>Kernel function to use. Currently supported: 'exp' (exponential decay). Future options: 'gaussian'.</p> <code>'exp'</code> <code>h_min</code> <code>float</code> <p>Minimum activation threshold. Weights below this value are set to zero and excluded from the sparse output. Controls the sparsity of nbr_w \u2014 lower h_min means broader (denser) neighborhoods.</p> <code>0.01</code> <p>Attributes:</p> Name Type Description <code>kind</code> <code>str</code> <p>Kernel function identifier.</p> <code>h_min</code> <code>float</code> <p>Activation threshold.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; kernel = NeighborKernel(kind='exp', h_min=0.01)\n&gt;&gt;&gt; nbr_w = kernel.compute(embedding.dist, rho=3.0)\n&gt;&gt;&gt; nbr_w.shape\n(M, M)\n</code></pre> Source code in <code>src/gtsom/kernel.py</code> <pre><code>def __init__(self, kind=\"exp\", h_min=0.01):\n    if kind not in self.SUPPORTED_KINDS:\n        raise ValueError(\n            f\"kind '{kind}' not supported. \"\n            f\"Choose from {self.SUPPORTED_KINDS}.\"\n        )\n    if not (0.0 &lt; h_min &lt; 1.0):\n        raise ValueError(\n            f\"h_min must be in (0, 1), got {h_min}.\"\n        )\n    self.kind = kind\n    self.h_min = h_min\n</code></pre>"},{"location":"api/#gtsom.kernel.NeighborKernel.compute","title":"compute","text":"<pre><code>compute(dist, rho)\n</code></pre> <p>Compute sparse neighborhood activation weights.</p> <p>For each prototype i, computes the activation weight for every other prototype j using the kernel function applied to dist[i, j] and rho[i]. Weights below h_min are zeroed out, and the result is returned as a sparse CSR matrix.</p> <p>Row i of the returned matrix contains the nonzero activation weights of prototype i's neighborhood. The nonzero column indices give the active neighbor prototypes, making a separate nbr_idx array unnecessary.</p> <p>Parameters:</p> Name Type Description Default <code>dist</code> <code>(ndarray, shape(M, M))</code> <p>Pairwise prototype distance matrix (from Embedding.dist).</p> required <code>rho</code> <code>float or array-like of shape (M,)</code> <p>Neighborhood bandwidth. A scalar is broadcast to all M prototypes. An array allows per-prototype bandwidths.</p> required <p>Returns:</p> Name Type Description <code>nbr_w</code> <code>(csr_matrix, shape(M, M))</code> <p>Sparse matrix of neighborhood activation weights. nbr_w[i] gives the activation weights for prototype i's active neighbors. Self-activation (diagonal) is included.</p> Source code in <code>src/gtsom/kernel.py</code> <pre><code>def compute(self, dist, rho):\n    \"\"\"\n    Compute sparse neighborhood activation weights.\n\n    For each prototype i, computes the activation weight for every\n    other prototype j using the kernel function applied to dist[i, j]\n    and rho[i]. Weights below h_min are zeroed out, and the result\n    is returned as a sparse CSR matrix.\n\n    Row i of the returned matrix contains the nonzero activation\n    weights of prototype i's neighborhood. The nonzero column indices\n    give the active neighbor prototypes, making a separate nbr_idx\n    array unnecessary.\n\n    Parameters\n    ----------\n    dist : np.ndarray, shape (M, M)\n        Pairwise prototype distance matrix (from Embedding.dist).\n    rho : float or array-like of shape (M,)\n        Neighborhood bandwidth. A scalar is broadcast to all M\n        prototypes. An array allows per-prototype bandwidths.\n\n    Returns\n    -------\n    nbr_w : scipy.sparse.csr_matrix, shape (M, M)\n        Sparse matrix of neighborhood activation weights.\n        nbr_w[i] gives the activation weights for prototype i's\n        active neighbors. Self-activation (diagonal) is included.\n    \"\"\"\n    M = dist.shape[0]\n    rho = self._broadcast_rho(rho, M)\n\n    if self.kind == \"exp\":\n        W = self._exp_kernel(dist, rho)\n\n    W[W &lt; self.h_min] = 0.0\n    return csr_matrix(W)\n</code></pre>"},{"location":"gtsom_mnist_tutorial/","title":"GTSOM Tutorial: Mapping MNIST Handwritten Digits","text":"In\u00a0[1]: Copied! <pre>import numpy as np\nimport matplotlib.pyplot as plt\nimport matplotlib.gridspec as gridspec\nfrom matplotlib.patches import Patch\nfrom sklearn.datasets import fetch_openml\nfrom sklearn.decomposition import PCA\nfrom sklearn.preprocessing import MinMaxScaler\n\nfrom gtsom import GTSOM\n\n%matplotlib inline\nplt.rcParams['figure.dpi'] = 120\n</pre> import numpy as np import matplotlib.pyplot as plt import matplotlib.gridspec as gridspec from matplotlib.patches import Patch from sklearn.datasets import fetch_openml from sklearn.decomposition import PCA from sklearn.preprocessing import MinMaxScaler  from gtsom import GTSOM  %matplotlib inline plt.rcParams['figure.dpi'] = 120 In\u00a0[2]: Copied! <pre># Fetch MNIST from OpenML (downloads ~11 MB on first run, cached thereafter)\nmnist = fetch_openml('mnist_784', version=1, as_frame=False, parser='auto')\nX_raw = mnist.data.astype(np.float32)   # (70000, 784)\ny_raw = mnist.target.astype(int)         # (70000,)\n\n# Use the last 10,000 samples \u2014 the standard balanced test split\nN = 10_000\nX_raw = X_raw[-N:]\ny     = y_raw[-N:]\n\nprint(f'Dataset: {X_raw.shape}')\nprint(f'Label counts: { {d: int((y==d).sum()) for d in range(10)} }')\n\n# Scale globally to [0, 1]\nscaler   = MinMaxScaler()\nX_scaled = scaler.fit_transform(X_raw).astype(np.float32)\nprint(f'Scaled range: [{X_scaled.min():.3f}, {X_scaled.max():.3f}]')\n</pre> # Fetch MNIST from OpenML (downloads ~11 MB on first run, cached thereafter) mnist = fetch_openml('mnist_784', version=1, as_frame=False, parser='auto') X_raw = mnist.data.astype(np.float32)   # (70000, 784) y_raw = mnist.target.astype(int)         # (70000,)  # Use the last 10,000 samples \u2014 the standard balanced test split N = 10_000 X_raw = X_raw[-N:] y     = y_raw[-N:]  print(f'Dataset: {X_raw.shape}') print(f'Label counts: { {d: int((y==d).sum()) for d in range(10)} }')  # Scale globally to [0, 1] scaler   = MinMaxScaler() X_scaled = scaler.fit_transform(X_raw).astype(np.float32) print(f'Scaled range: [{X_scaled.min():.3f}, {X_scaled.max():.3f}]') <pre>Dataset: (10000, 784)\nLabel counts: {0: 980, 1: 1135, 2: 1032, 3: 1010, 4: 982, 5: 892, 6: 958, 7: 1028, 8: 974, 9: 1009}\nScaled range: [0.000, 1.000]\n</pre> In\u00a0[3]: Copied! <pre>N_PCS = 50\npca   = PCA(n_components=N_PCS, svd_solver='randomized', random_state=42)\nX     = pca.fit_transform(X_scaled).astype(np.float32)\nprint(f'Reduced: {X_scaled.shape} -&gt; {X.shape}')\nprint(f'Variance explained by {N_PCS} PCs: {pca.explained_variance_ratio_.sum()*100:.1f}%')\n</pre> N_PCS = 50 pca   = PCA(n_components=N_PCS, svd_solver='randomized', random_state=42) X     = pca.fit_transform(X_scaled).astype(np.float32) print(f'Reduced: {X_scaled.shape} -&gt; {X.shape}') print(f'Variance explained by {N_PCS} PCs: {pca.explained_variance_ratio_.sum()*100:.1f}%') <pre>Reduced: (10000, 784) -&gt; (10000, 50)\nVariance explained by 50 PCs: 83.1%\n</pre> In\u00a0[4]: Copied! <pre># Scree plot: cumulative variance explained\nfig, ax = plt.subplots(figsize=(7, 3))\ncumvar = np.cumsum(pca.explained_variance_ratio_) * 100\nax.plot(range(1, N_PCS + 1), cumvar, 'o-', ms=3, lw=1.5, color='steelblue')\nax.axhline(85, color='dimgrey', lw=0.8, linestyle='--', label='85% variance')\nax.axhline(95, color='coral',   lw=0.8, linestyle='--', label='95% variance')\nax.set_xlabel('Number of principal components')\nax.set_ylabel('Cumulative variance explained (%)')\nax.set_title('MNIST PCA \u2014 Cumulative Variance Explained')\nax.legend(fontsize=8)\nax.grid(True, alpha=0.3)\nplt.tight_layout()\nplt.show()\n</pre> # Scree plot: cumulative variance explained fig, ax = plt.subplots(figsize=(7, 3)) cumvar = np.cumsum(pca.explained_variance_ratio_) * 100 ax.plot(range(1, N_PCS + 1), cumvar, 'o-', ms=3, lw=1.5, color='steelblue') ax.axhline(85, color='dimgrey', lw=0.8, linestyle='--', label='85% variance') ax.axhline(95, color='coral',   lw=0.8, linestyle='--', label='95% variance') ax.set_xlabel('Number of principal components') ax.set_ylabel('Cumulative variance explained (%)') ax.set_title('MNIST PCA \u2014 Cumulative Variance Explained') ax.legend(fontsize=8) ax.grid(True, alpha=0.3) plt.tight_layout() plt.show() In\u00a0[5]: Copied! <pre>SEED = 42\n\nsom = GTSOM.from_grid(\n    X,\n    shape=(20, 20),\n    coord_init='hex',\n    W_init='random',\n    random_state=SEED,\n    labels=y,\n)\nprint(som)\nprint(f'Prototypes with empty receptive fields at init: '\n      f'{(som.recaller.RFSize == 0).sum()} / {som.M}')\n</pre> SEED = 42  som = GTSOM.from_grid(     X,     shape=(20, 20),     coord_init='hex',     W_init='random',     random_state=SEED,     labels=y, ) print(som) print(f'Prototypes with empty receptive fields at init: '       f'{(som.recaller.RFSize == 0).sum()} / {som.M}') <pre>GTSOM(M=400, d=50, embed_dim=2, compiled=False, fitted=False, age=0, snapshots=1, backend='numba', n_jobs=None, train_time=0.000s)\nPrototypes with empty receptive fields at init: 0 / 400\n</pre> <p>At initialisation, prototypes are placed randomly within the data cloud and the output lattice has no meaningful organisation \u2014 digit labels are distributed randomly across the hex grid, as the plot below shows.</p> In\u00a0[6]: Copied! <pre>fig_init = som.plot(\n    color_by='labels',\n    title='GTSOM \u2014 MNIST (20\u00d720 hex)',\n    subtitle='Initial state (age=0)',\n)\nplt.show()\n</pre> fig_init = som.plot(     color_by='labels',     title='GTSOM \u2014 MNIST (20\u00d720 hex)',     subtitle='Initial state (age=0)', ) plt.show() In\u00a0[7]: Copied! <pre>som.compile(\n    rho_0=10.0,\n    rho_f=1.0,\n    target_epochs=100,\n    n_jobs=-1,   # use all available CPU cores\n)\n\n# Visualise the bandwidth decay schedule\nrho_vals = som.rho_schedule.values(200)\nfig, ax  = plt.subplots(figsize=(7, 3))\nax.plot(range(200), rho_vals, lw=2, color='steelblue')\nax.axhline(1.0,  color='coral',   lw=0.8, linestyle='--', label='rho_f = 1.0')\nax.axvline(100,  color='dimgrey', lw=0.8, linestyle='--', label='target_epochs = 100')\nax.set_xlabel('Epoch')\nax.set_ylabel('Neighbourhood bandwidth \u03c1')\nax.set_title('Exponential bandwidth decay schedule')\nax.legend(fontsize=8)\nax.grid(True, alpha=0.3)\nplt.tight_layout()\nplt.show()\n</pre> som.compile(     rho_0=10.0,     rho_f=1.0,     target_epochs=100,     n_jobs=-1,   # use all available CPU cores )  # Visualise the bandwidth decay schedule rho_vals = som.rho_schedule.values(200) fig, ax  = plt.subplots(figsize=(7, 3)) ax.plot(range(200), rho_vals, lw=2, color='steelblue') ax.axhline(1.0,  color='coral',   lw=0.8, linestyle='--', label='rho_f = 1.0') ax.axvline(100,  color='dimgrey', lw=0.8, linestyle='--', label='target_epochs = 100') ax.set_xlabel('Epoch') ax.set_ylabel('Neighbourhood bandwidth \u03c1') ax.set_title('Exponential bandwidth decay schedule') ax.legend(fontsize=8) ax.grid(True, alpha=0.3) plt.tight_layout() plt.show() In\u00a0[8]: Copied! <pre>som.fit(\n    X,\n    n_epochs=200,\n    labels=y,\n    verbose=True,\n    plot_every=25,\n)\nprint(f'\\nFinal state: {som}')\nprint(f'Empty RFs after training: {(som.recaller.RFSize == 0).sum()} / {som.M}')\n</pre> som.fit(     X,     n_epochs=200,     labels=y,     verbose=True,     plot_every=25, ) print(f'\\nFinal state: {som}') print(f'Empty RFs after training: {(som.recaller.RFSize == 0).sum()} / {som.M}') <pre>  Epoch      rho        MQE    delBMU\n  -----   ------   --------   -------\n      1   10.0000     6.5607    0.9437\n      2   9.8855     6.2517    0.5883\n      3   9.7724     6.1914    0.4608\n      4   9.6605     6.1689    0.3789\n      5   9.5499     6.1551    0.3245\n      6   9.4406     6.1417    0.2573\n      7   9.3325     6.1283    0.2614\n      8   9.2257     6.1085    0.3453\n      9   9.1201     6.0842    0.3624\n     10   9.0157     6.0677    0.3029\n  -----   ------   --------   -------\n     11   8.9125     6.0567    0.2463\n     12   8.8105     6.0469    0.2373\n     13   8.7096     6.0378    0.2168\n     14   8.6099     6.0300    0.1981\n     15   8.5114     6.0229    0.1784\n     16   8.4140     6.0162    0.1758\n     17   8.3176     6.0095    0.1618\n     18   8.2224     6.0027    0.1514\n     19   8.1283     5.9961    0.1370\n     20   8.0353     5.9898    0.1299\n  -----   ------   --------   -------\n     21   7.9433     5.9834    0.1219\n     22   7.8524     5.9770    0.1080\n     23   7.7625     5.9708    0.1006\n     24   7.6736     5.9647    0.0933\n     25   7.5858     5.9586    0.0903\n     26   7.4989     5.9525    0.0833\n     27   7.4131     5.9466    0.0693\n     28   7.3282     5.9407    0.0677\n     29   7.2444     5.9349    0.0706\n     30   7.1614     5.9290    0.0660\n  -----   ------   --------   -------\n     31   7.0795     5.9231    0.0677\n     32   6.9984     5.9170    0.0647\n     33   6.9183     5.9108    0.0659\n     34   6.8391     5.9046    0.0554\n     35   6.7608     5.8985    0.0575\n     36   6.6834     5.8923    0.0511\n     37   6.6069     5.8862    0.0463\n     38   6.5313     5.8801    0.0458\n     39   6.4565     5.8740    0.0459\n     40   6.3826     5.8679    0.0427\n  -----   ------   --------   -------\n     41   6.3096     5.8618    0.0433\n     42   6.2373     5.8556    0.0424\n     43   6.1660     5.8495    0.0420\n     44   6.0954     5.8432    0.0436\n     45   6.0256     5.8365    0.0464\n     46   5.9566     5.8302    0.0450\n     47   5.8884     5.8239    0.0406\n     48   5.8210     5.8173    0.0465\n     49   5.7544     5.8109    0.0407\n     50   5.6885     5.8045    0.0381\n  -----   ------   --------   -------\n     51   5.6234     5.7979    0.0445\n     52   5.5590     5.7915    0.0363\n     53   5.4954     5.7851    0.0370\n     54   5.4325     5.7787    0.0395\n     55   5.3703     5.7718    0.0431\n     56   5.3088     5.7652    0.0392\n     57   5.2481     5.7587    0.0408\n     58   5.1880     5.7514    0.0512\n     59   5.1286     5.7446    0.0501\n     60   5.0699     5.7375    0.0480\n  -----   ------   --------   -------\n     61   5.0119     5.7303    0.0513\n     62   4.9545     5.7222    0.0590\n     63   4.8978     5.7149    0.0523\n     64   4.8417     5.7076    0.0516\n     65   4.7863     5.7000    0.0536\n     66   4.7315     5.6913    0.0675\n     67   4.6774     5.6828    0.0603\n     68   4.6238     5.6743    0.0594\n     69   4.5709     5.6662    0.0524\n     70   4.5186     5.6564    0.0543\n  -----   ------   --------   -------\n     71   4.4668     5.6488    0.0420\n     72   4.4157     5.6413    0.0373\n     73   4.3652     5.6338    0.0367\n     74   4.3152     5.6246    0.0417\n     75   4.2658     5.6172    0.0325\n     76   4.2170     5.6098    0.0273\n     77   4.1687     5.6024    0.0301\n     78   4.1210     5.5903    0.0474\n     79   4.0738     5.5831    0.0299\n     80   4.0272     5.5760    0.0275\n  -----   ------   --------   -------\n     81   3.9811     5.5687    0.0294\n     82   3.9355     5.5613    0.0282\n     83   3.8905     5.5503    0.0536\n     84   3.8459     5.5427    0.0332\n     85   3.8019     5.5353    0.0303\n     86   3.7584     5.5278    0.0337\n     87   3.7154     5.5202    0.0333\n     88   3.6728     5.5088    0.0596\n     89   3.6308     5.5005    0.0423\n     90   3.5892     5.4923    0.0389\n  -----   ------   --------   -------\n     91   3.5481     5.4842    0.0372\n     92   3.5075     5.4761    0.0372\n     93   3.4674     5.4647    0.0581\n     94   3.4277     5.4565    0.0345\n     95   3.3884     5.4487    0.0306\n     96   3.3497     5.4408    0.0286\n     97   3.3113     5.4329    0.0304\n     98   3.2734     5.4248    0.0343\n     99   3.2359     5.4131    0.0554\n    100   3.1989     5.4048    0.0371\n  -----   ------   --------   -------\n    101   3.1623     5.3967    0.0304\n    102   3.1261     5.3886    0.0280\n    103   3.0903     5.3805    0.0272\n    104   3.0549     5.3724    0.0264\n    105   3.0200     5.3601    0.0557\n    106   2.9854     5.3514    0.0364\n    107   2.9512     5.3431    0.0292\n    108   2.9174     5.3347    0.0293\n    109   2.8840     5.3263    0.0310\n    110   2.8510     5.3180    0.0283\n  -----   ------   --------   -------\n    111   2.8184     5.3044    0.0533\n    112   2.7861     5.2960    0.0317\n    113   2.7542     5.2879    0.0283\n    114   2.7227     5.2796    0.0279\n    115   2.6915     5.2714    0.0240\n    116   2.6607     5.2633    0.0232\n    117   2.6303     5.2551    0.0224\n    118   2.6002     5.2405    0.0524\n    119   2.5704     5.2318    0.0318\n    120   2.5410     5.2233    0.0280\n  -----   ------   --------   -------\n    121   2.5119     5.2149    0.0265\n    122   2.4831     5.2066    0.0227\n    123   2.4547     5.1983    0.0205\n    124   2.4266     5.1900    0.0230\n    125   2.3988     5.1815    0.0264\n    126   2.3714     5.1658    0.0501\n    127   2.3442     5.1570    0.0345\n    128   2.3174     5.1485    0.0285\n    129   2.2909     5.1399    0.0284\n    130   2.2646     5.1315    0.0254\n  -----   ------   --------   -------\n    131   2.2387     5.1232    0.0220\n    132   2.2131     5.1148    0.0197\n    133   2.1878     5.1065    0.0184\n    134   2.1627     5.0892    0.0431\n    135   2.1380     5.0805    0.0304\n    136   2.1135     5.0721    0.0277\n    137   2.0893     5.0637    0.0265\n    138   2.0654     5.0551    0.0276\n    139   2.0417     5.0465    0.0291\n    140   2.0184     5.0378    0.0271\n  -----   ------   --------   -------\n    141   1.9953     5.0293    0.0272\n    142   1.9724     5.0208    0.0264\n    143   1.9498     5.0014    0.0475\n    144   1.9275     4.9928    0.0394\n    145   1.9055     4.9841    0.0341\n    146   1.8836     4.9754    0.0320\n    147   1.8621     4.9669    0.0271\n    148   1.8408     4.9585    0.0260\n    149   1.8197     4.9501    0.0232\n    150   1.7989     4.9420    0.0213\n  -----   ------   --------   -------\n    151   1.7783     4.9339    0.0200\n    152   1.7579     4.9257    0.0195\n    153   1.7378     4.9176    0.0219\n    154   1.7179     4.8968    0.0424\n    155   1.6982     4.8883    0.0300\n    156   1.6788     4.8802    0.0249\n    157   1.6596     4.8723    0.0241\n    158   1.6406     4.8640    0.0245\n    159   1.6218     4.8556    0.0227\n    160   1.6032     4.8474    0.0192\n  -----   ------   --------   -------\n    161   1.5849     4.8393    0.0183\n    162   1.5668     4.8314    0.0209\n    163   1.5488     4.8232    0.0206\n    164   1.5311     4.8148    0.0206\n    165   1.5136     4.7915    0.0424\n    166   1.4962     4.7826    0.0303\n    167   1.4791     4.7743    0.0246\n    168   1.4622     4.7664    0.0204\n    169   1.4454     4.7588    0.0191\n    170   1.4289     4.7512    0.0176\n  -----   ------   --------   -------\n    171   1.4125     4.7436    0.0148\n    172   1.3964     4.7361    0.0148\n    173   1.3804     4.7286    0.0155\n    174   1.3646     4.7209    0.0146\n    175   1.3490     4.7134    0.0156\n    176   1.3335     4.7058    0.0149\n    177   1.3183     4.6980    0.0140\n    178   1.3032     4.6902    0.0130\n    179   1.2882     4.6654    0.0358\n    180   1.2735     4.6571    0.0232\n  -----   ------   --------   -------\n    181   1.2589     4.6492    0.0195\n    182   1.2445     4.6415    0.0159\n    183   1.2303     4.6339    0.0145\n    184   1.2162     4.6265    0.0124\n    185   1.2023     4.6193    0.0133\n    186   1.1885     4.6118    0.0128\n    187   1.1749     4.6044    0.0130\n    188   1.1614     4.5969    0.0124\n    189   1.1482     4.5895    0.0109\n    190   1.1350     4.5820    0.0120\n  -----   ------   --------   -------\n    191   1.1220     4.5745    0.0119\n    192   1.1092     4.5669    0.0109\n    193   1.0965     4.5596    0.0104\n    194   1.0839     4.5308    0.0352\n    195   1.0715     4.5224    0.0196\n    196   1.0593     4.5150    0.0165\n    197   1.0471     4.5078    0.0139\n    198   1.0351     4.5006    0.0125\n    199   1.0233     4.4936    0.0117\n    200   1.0116     4.4865    0.0118\n  -----   ------   --------   -------\n  -----   ------   --------   -------\n\nFinal state: GTSOM(M=400, d=50, embed_dim=2, compiled=True, fitted=True, age=200, snapshots=201, backend='numba', n_jobs=16, train_time=83.581s)\nEmpty RFs after training: 0 / 400\n</pre> In\u00a0[9]: Copied! <pre>ages_arr   = np.array([s['age']    for s in som.learn_history_])\nmqes_arr   = np.array([s['mqe']    for s in som.learn_history_])\ndelbmu_arr = np.array([s['delBMU'] for s in som.learn_history_])\n\nfig, (ax_mqe, ax_del) = plt.subplots(1, 2, figsize=(13, 4))\n\nax_mqe.plot(ages_arr, mqes_arr, 'o-', lw=1.5, ms=3, color='steelblue')\nax_mqe.axvline(0, color='dimgrey', lw=0.8, linestyle='--', label='Init (age=0)')\nax_mqe.set_xlabel('Age (epochs)')\nax_mqe.set_ylabel('Global MQE')\nax_mqe.set_title('SOM Learning\\nGlobal MQE')\nax_mqe.legend(fontsize=8)\nax_mqe.grid(True, alpha=0.3)\n\nax_del.plot(ages_arr, delbmu_arr, 'o-', lw=1.5, ms=3, color='darkorange')\nax_del.axvline(0, color='dimgrey', lw=0.8, linestyle='--', label='Init (age=0)')\nax_del.set_xlabel('Age (epochs)')\nax_del.set_ylabel('delBMU')\nax_del.set_title('SOM Learning\\nProportion of changed BMUs')\nax_del.set_ylim(-0.05, 1.05)\nax_del.legend(fontsize=8)\nax_del.grid(True, alpha=0.3)\n\nplt.tight_layout()\nplt.show()\n</pre> ages_arr   = np.array([s['age']    for s in som.learn_history_]) mqes_arr   = np.array([s['mqe']    for s in som.learn_history_]) delbmu_arr = np.array([s['delBMU'] for s in som.learn_history_])  fig, (ax_mqe, ax_del) = plt.subplots(1, 2, figsize=(13, 4))  ax_mqe.plot(ages_arr, mqes_arr, 'o-', lw=1.5, ms=3, color='steelblue') ax_mqe.axvline(0, color='dimgrey', lw=0.8, linestyle='--', label='Init (age=0)') ax_mqe.set_xlabel('Age (epochs)') ax_mqe.set_ylabel('Global MQE') ax_mqe.set_title('SOM Learning\\nGlobal MQE') ax_mqe.legend(fontsize=8) ax_mqe.grid(True, alpha=0.3)  ax_del.plot(ages_arr, delbmu_arr, 'o-', lw=1.5, ms=3, color='darkorange') ax_del.axvline(0, color='dimgrey', lw=0.8, linestyle='--', label='Init (age=0)') ax_del.set_xlabel('Age (epochs)') ax_del.set_ylabel('delBMU') ax_del.set_title('SOM Learning\\nProportion of changed BMUs') ax_del.set_ylim(-0.05, 1.05) ax_del.legend(fontsize=8) ax_del.grid(True, alpha=0.3)  plt.tight_layout() plt.show() In\u00a0[10]: Copied! <pre># Re-run plot() for each target age, passing each subplot ax directly.\n# The learn_history_ snapshots tell us what age to label each panel,\n# and temporarily swapping som.W / som.recaller state is unnecessary \u2014\n# we instead read the stored figure from each snapshot and display it\n# as an image, which is the cleanest approach for a 2x2 grid.\ntarget_ages = [0, 50, 100, 200]\nsnap_by_age = {s['age']: s for s in som.learn_history_ if s['fig'] is not None}\n\nfig, axes = plt.subplots(2, 2, figsize=(14, 12))\nfig.suptitle('GTSOM \u2014 MNIST lattice organisation over training',\n             fontsize=13, fontweight='bold', y=1.01)\n\nfor ax, age in zip(axes.flat, target_ages):\n    snap_fig = snap_by_age[age]['fig']\n    # Render stored figure to a pixel array and display as image\n    snap_fig.canvas.draw()\n    buf = snap_fig.canvas.buffer_rgba()\n    img = np.asarray(buf)\n    ax.imshow(img)\n    ax.axis('off')\n    ax.set_title(f'Age = {age}', fontsize=12, pad=6)\n\nplt.tight_layout()\nplt.show()\n</pre> # Re-run plot() for each target age, passing each subplot ax directly. # The learn_history_ snapshots tell us what age to label each panel, # and temporarily swapping som.W / som.recaller state is unnecessary \u2014 # we instead read the stored figure from each snapshot and display it # as an image, which is the cleanest approach for a 2x2 grid. target_ages = [0, 50, 100, 200] snap_by_age = {s['age']: s for s in som.learn_history_ if s['fig'] is not None}  fig, axes = plt.subplots(2, 2, figsize=(14, 12)) fig.suptitle('GTSOM \u2014 MNIST lattice organisation over training',              fontsize=13, fontweight='bold', y=1.01)  for ax, age in zip(axes.flat, target_ages):     snap_fig = snap_by_age[age]['fig']     # Render stored figure to a pixel array and display as image     snap_fig.canvas.draw()     buf = snap_fig.canvas.buffer_rgba()     img = np.asarray(buf)     ax.imshow(img)     ax.axis('off')     ax.set_title(f'Age = {age}', fontsize=12, pad=6)  plt.tight_layout() plt.show() In\u00a0[11]: Copied! <pre>fig = som.plot(\n    color_by='labels',\n    title='GTSOM \u2014 MNIST (20\u00d720 hex)',\n    subtitle=f'age={som.age}, color_by=\\'labels\\'',\n)\nplt.show()\n</pre> fig = som.plot(     color_by='labels',     title='GTSOM \u2014 MNIST (20\u00d720 hex)',     subtitle=f'age={som.age}, color_by=\\'labels\\'', ) plt.show() In\u00a0[12]: Copied! <pre>fig, axes = plt.subplots(1, 2, figsize=(14, 6))\n\nfor ax, color_by in zip(axes, ['mqe', 'rfsize']):\n    inner_fig = som.plot(\n        color_by=color_by,\n        title='GTSOM \u2014 MNIST',\n        subtitle=f'color_by=\\'{color_by}\\'',\n        ax=ax,\n    )\n\nplt.tight_layout()\nplt.show()\n</pre> fig, axes = plt.subplots(1, 2, figsize=(14, 6))  for ax, color_by in zip(axes, ['mqe', 'rfsize']):     inner_fig = som.plot(         color_by=color_by,         title='GTSOM \u2014 MNIST',         subtitle=f'color_by=\\'{color_by}\\'',         ax=ax,     )  plt.tight_layout() plt.show() In\u00a0[13]: Copied! <pre># Back-project W from PCA score space to pixel space\nW_pixels = pca.inverse_transform(som.W)          # (M, 784)\nW_pixels = scaler.inverse_transform(W_pixels)    # undo [0,1] scaling\nW_pixels = np.clip(W_pixels, 0, 255).astype(np.uint8)\n\ncoords  = som.embed.coords   # (M, 2) hex lattice positions\ntile    = 28\nx_min, x_max = coords[:, 0].min(), coords[:, 0].max()\ny_min, y_max = coords[:, 1].min(), coords[:, 1].max()\nx_range = x_max - x_min\ny_range = y_max - y_min\n\ncanvas_w = int(20 * tile * 1.15)\ncanvas_h = int(20 * tile * 1.25)\ncanvas   = np.ones((canvas_h, canvas_w), dtype=np.uint8) * 255\n\nfor m in range(som.M):\n    px = int((coords[m, 0] - x_min) / x_range * (canvas_w - tile))\n    py = int((coords[m, 1] - y_min) / y_range * (canvas_h - tile))\n    py = canvas_h - tile - py   # flip y\n    py = max(0, min(py, canvas_h - tile))\n    px = max(0, min(px, canvas_w - tile))\n    canvas[py:py+tile, px:px+tile] = W_pixels[m].reshape(28, 28)\n\nfig, ax = plt.subplots(figsize=(12, 10))\nax.imshow(canvas, cmap='gray_r', vmin=0, vmax=255)\nax.axis('off')\nax.set_title(\n    'GTSOM \u2014 MNIST Prototype Images\\n'\n    '(each neuron\\'s weight vector back-projected to pixel space)',\n    fontsize=11,\n)\nplt.tight_layout()\nplt.show()\n</pre> # Back-project W from PCA score space to pixel space W_pixels = pca.inverse_transform(som.W)          # (M, 784) W_pixels = scaler.inverse_transform(W_pixels)    # undo [0,1] scaling W_pixels = np.clip(W_pixels, 0, 255).astype(np.uint8)  coords  = som.embed.coords   # (M, 2) hex lattice positions tile    = 28 x_min, x_max = coords[:, 0].min(), coords[:, 0].max() y_min, y_max = coords[:, 1].min(), coords[:, 1].max() x_range = x_max - x_min y_range = y_max - y_min  canvas_w = int(20 * tile * 1.15) canvas_h = int(20 * tile * 1.25) canvas   = np.ones((canvas_h, canvas_w), dtype=np.uint8) * 255  for m in range(som.M):     px = int((coords[m, 0] - x_min) / x_range * (canvas_w - tile))     py = int((coords[m, 1] - y_min) / y_range * (canvas_h - tile))     py = canvas_h - tile - py   # flip y     py = max(0, min(py, canvas_h - tile))     px = max(0, min(px, canvas_w - tile))     canvas[py:py+tile, px:px+tile] = W_pixels[m].reshape(28, 28)  fig, ax = plt.subplots(figsize=(12, 10)) ax.imshow(canvas, cmap='gray_r', vmin=0, vmax=255) ax.axis('off') ax.set_title(     'GTSOM \u2014 MNIST Prototype Images\\n'     '(each neuron\\'s weight vector back-projected to pixel space)',     fontsize=11, ) plt.tight_layout() plt.show()"},{"location":"gtsom_mnist_tutorial/#gtsom-tutorial-mapping-mnist-handwritten-digits","title":"GTSOM Tutorial: Mapping MNIST Handwritten Digits\u00b6","text":""},{"location":"gtsom_mnist_tutorial/#what-is-a-self-organising-map","title":"What is a Self-Organising Map?\u00b6","text":"<p>A Self-Organising Map (SOM) is an unsupervised learning algorithm that projects high-dimensional data onto a low-dimensional output lattice \u2014 typically a 2D grid \u2014 while preserving the topological structure of the data. Neurons that are nearby in the output lattice learn to respond to similar inputs, so the map can be read as a kind of 'neighbourhood diagram' of the data: regions of the lattice that are close together represent data that are similar in the original high-dimensional space.</p> <p>Unlike dimensionality reduction methods such as PCA or UMAP, the SOM output is a fixed, structured lattice whose geometry is chosen by the user. This makes the result straightforward to visualise and interpret, and allows the user to impose domain-specific structure on the output space.</p> <p>GTSOM (General Topology SOM) extends the classical SOM by allowing arbitrary output space geometries, defined by any graph structure \u2014 hexagonal grids, rectangular grids, or data-driven Delaunay triangulations built from the data itself. This tutorial demonstrates the hexagonal grid constructor (<code>GTSOM.from_grid</code>) on the MNIST handwritten digits dataset.</p>"},{"location":"gtsom_mnist_tutorial/#what-we-will-do","title":"What we will do\u00b6","text":"<ol> <li>Load and preprocess MNIST (10,000 images, scaled to [0,1], reduced to 50 principal components)</li> <li>Initialise a 20\u00d720 hexagonal GTSOM</li> <li>Train for 200 epochs with an exponentially decaying neighbourhood bandwidth</li> <li>Visualise the learned map: digit organisation, quantization error, and prototype images</li> </ol>"},{"location":"gtsom_mnist_tutorial/#1-imports","title":"1. Imports\u00b6","text":""},{"location":"gtsom_mnist_tutorial/#2-loading-mnist","title":"2. Loading MNIST\u00b6","text":"<p>MNIST contains 70,000 28\u00d728 greyscale images of handwritten digits (0\u20139), each flattened to a 784-dimensional vector. We use the last 10,000 samples, which form the standard test split and are approximately balanced across all 10 digit classes.</p> <p>Pixel values are scaled globally to [0, 1] before any further processing. GTSOM does not perform any internal scaling \u2014 preprocessing is the user's responsibility, and consistent scaling ensures that all input dimensions contribute equally to the nearest-neighbour search.</p>"},{"location":"gtsom_mnist_tutorial/#3-dimensionality-reduction-via-pca","title":"3. Dimensionality Reduction via PCA\u00b6","text":"<p>With d=784 input dimensions, computing nearest-neighbour distances for each data point at every training epoch would be expensive and dominated by uninformative dimensions (the blank border pixels common to all images). We reduce to 50 principal components, which captures approximately 85% of the total variance while dramatically reducing computation time.</p> <p>This PCA is a preprocessing step applied to the raw pixel data. The SOM then operates in this 50-dimensional PCA score space.</p>"},{"location":"gtsom_mnist_tutorial/#4-choosing-the-output-lattice","title":"4. Choosing the Output Lattice\u00b6","text":""},{"location":"gtsom_mnist_tutorial/#lattice-size","title":"Lattice size\u00b6","text":"<p>A common rule of thumb for SOM size is M \u2248 5\u221aN neurons, where N is the number of training samples. For N=10,000 this gives M \u2248 500, suggesting a grid of roughly 22\u00d722. We use a 20\u00d720 grid (M=400), which is slightly more conservative but trains faster and remains visually interpretable. With 400 neurons and 10,000 data points, each neuron will serve on average 25 data points in its receptive field.</p> <p>Too few neurons: multiple digit classes are forced to share prototypes, blurring the map. Too many neurons: training is slow and many neurons end up with empty receptive fields.</p>"},{"location":"gtsom_mnist_tutorial/#hexagonal-vs-rectangular-grid","title":"Hexagonal vs rectangular grid\u00b6","text":"<p>We use a hexagonal grid (<code>coord_init='hex'</code>) rather than rectangular. In a hex grid every interior neuron has exactly 6 equidistant neighbours, giving a more uniform neighbourhood structure than a rectangular grid where diagonal neighbours are further away than axis-aligned ones. This tends to produce smoother, more isotropic maps.</p>"},{"location":"gtsom_mnist_tutorial/#weight-initialisation","title":"Weight initialisation\u00b6","text":"<p><code>W_init='random'</code> initialises each prototype by sampling a random data point from X. This gives a starting configuration that is well-distributed within the data cloud. The SOM training then moves prototypes from these initial positions into the regions of the data space they will ultimately represent.</p>"},{"location":"gtsom_mnist_tutorial/#5-initialising-the-gtsom","title":"5. Initialising the GTSOM\u00b6","text":""},{"location":"gtsom_mnist_tutorial/#6-the-learning-schedule","title":"6. The Learning Schedule\u00b6","text":"<p>Before training, we configure the neighbourhood bandwidth schedule \u2014 the parameter \u03c1 (rho) that controls how widely each prototype update propagates across the lattice.</p>"},{"location":"gtsom_mnist_tutorial/#how-the-som-update-rule-works","title":"How the SOM update rule works\u00b6","text":"<p>At each training epoch, every data point is assigned to its nearest prototype (the Best Matching Unit, or BMU). The BMU and its neighbours are then pulled toward the data points assigned to the BMU. The neighbourhood influence of prototype j on prototype b's update is determined by a kernel weight h(dist(b,j), \u03c1) that falls off with geodesic distance on the lattice. A large \u03c1 means a wide neighbourhood: many prototypes move together, producing coarse global organisation. A small \u03c1 means only immediate neighbours are affected, producing fine local refinement.</p>"},{"location":"gtsom_mnist_tutorial/#parameter-choices","title":"Parameter choices\u00b6","text":"<p><code>rho_0 = 10.0</code> \u2014 initial bandwidth. The 20\u00d720 hex lattice has a diameter of approximately 20 geodesic hops. Starting with \u03c1=10 means each prototype's neighbourhood initially spans roughly half the lattice, ensuring the first training epochs establish global organisation rather than local rearrangement.</p> <p><code>rho_f = 1.0</code> \u2014 final bandwidth. At \u03c1=1, only immediately adjacent neurons (6 neighbours in a hex grid) influence each other's updates. This is the standard SOM minimum: the map continues to refine locally but the global topology is fixed.</p> <p><code>target_epochs = 100</code>. The bandwidth decays exponentially from \u03c1\u2080 to \u03c1_f; <code>target_epochs</code> sets the half-life of this decay (the geometric midpoint between \u03c1\u2080 and \u03c1_f is reached at <code>target_epochs</code>, and \u03c1_f is reached at <code>2 \u00d7 target_epochs</code>). With 200 total epochs and <code>target_epochs=100</code>, the bandwidth is actively decaying throughout the first half of training and at its floor for the second half \u2014 a natural two-phase structure: global organisation followed by local refinement.</p> <p><code>n_jobs=-1</code> enables parallel prototype updates using all available CPU cores via a numba JIT-compiled kernel, significantly accelerating training on large datasets.</p>"},{"location":"gtsom_mnist_tutorial/#7-training","title":"7. Training\u00b6","text":"<p>We train for 200 epochs. A snapshot of the SOM state is captured every 25 epochs and stored in <code>learn_history_</code> for retrospective inspection. The verbose output below shows the epoch number, current bandwidth \u03c1, global mean quantization error (MQE), and the proportion of data points whose nearest prototype changed since the previous epoch (delBMU).</p>"},{"location":"gtsom_mnist_tutorial/#8-learning-curves","title":"8. Learning Curves\u00b6","text":"<p>Two quantities are monitored at every epoch:</p> <p>Mean Quantization Error (MQE) is the average Euclidean distance from each data point to its nearest prototype. It measures how well the prototypes represent the data \u2014 lower is better. MQE generally decreases monotonically as training progresses.</p> <p>delBMU is the proportion of data points whose nearest prototype (BMU) changed from the previous epoch. It measures how much the map is still reorganising \u2014 a value near 0 indicates convergence. The periodic spikes visible in the delBMU curve reflect neighbourhood shell collapses: as \u03c1 decays, the kernel weight for prototypes at distance d geodesic hops drops below a minimum threshold, effectively removing that shell from the neighbourhood. Each such collapse triggers a brief burst of BMU reassignments before the map re-settles.</p>"},{"location":"gtsom_mnist_tutorial/#9-lattice-organisation-over-time","title":"9. Lattice Organisation Over Time\u00b6","text":"<p>The following four panels show the SOM lattice coloured by the plurality digit label at each neuron, captured at ages 0, 50, 100, and 200. At age 0 the labels are randomly distributed. By age 50 a coarse global structure is already visible \u2014 similar digits are beginning to cluster spatially. The map continues to sharpen through the remaining epochs as the neighbourhood bandwidth narrows and prototypes refine their local territories.</p>"},{"location":"gtsom_mnist_tutorial/#10-final-learned-map","title":"10. Final Learned Map\u00b6","text":""},{"location":"gtsom_mnist_tutorial/#digit-organisation","title":"Digit organisation\u00b6","text":"<p>The map below shows the final trained SOM coloured by the plurality digit label at each neuron. The SOM has learned a spatially coherent organisation of the digit classes \u2014 similar-looking digits occupy neighbouring regions of the lattice.</p> <p>The topology reflects the visual similarity structure of the digits: curved digits (0, 6, 8, 9) tend to cluster together, as do straight-stroked digits (1, 4, 7). Digits with high intra-class variability (notably 5, which can resemble 3, 6, or 8 depending on the writer) may appear scattered or occupy boundary regions between other digit clusters.</p>"},{"location":"gtsom_mnist_tutorial/#quantization-error-and-receptive-field-size","title":"Quantization error and receptive field size\u00b6","text":"<p>Two further diagnostic views of the trained map:</p> <p>Mean Quantization Error per neuron (<code>color_by='mqe'</code>) shows where the map fits the data well (low MQE, dark) and where prototypes are further from their assigned data points (high MQE, bright). High-MQE regions often correspond to class boundaries where a single prototype must represent data from multiple nearby clusters.</p> <p>Receptive field size (<code>color_by='rfsize'</code>) shows how many data points are assigned to each neuron. A uniform distribution across neurons is ideal; neurons with very large receptive fields are 'overloaded' and may benefit from a larger lattice.</p>"},{"location":"gtsom_mnist_tutorial/#11-prototype-images","title":"11. Prototype Images\u00b6","text":"<p>Each neuron's weight vector W[j] is a point in 50-dimensional PCA score space. By applying the inverse PCA transform, we can reconstruct the 28\u00d728 pixel image that each neuron has learned to represent \u2014 the 'template' for its region of the digit space. Arranging these prototype images on the hex lattice produces a visual summary of the entire learned map.</p> <p>Prototypes in the interior of a digit cluster should resemble that digit clearly; prototypes at boundaries between clusters will show blended or ambiguous forms \u2014 a natural consequence of the SOM's smooth topographic mapping.</p>"},{"location":"gtsom_mnist_tutorial/#12-summary","title":"12. Summary\u00b6","text":"<p>In this tutorial we trained a 20\u00d720 hexagonal GTSOM on 10,000 MNIST digit images reduced to 50 principal components. The trained map shows clear topographic organisation: digit classes occupy contiguous regions of the lattice, and the spatial relationships between regions reflect the visual similarity of the digits.</p> <p>Key parameters and their roles:</p> Parameter Value Role Grid shape 20\u00d720 hex M=400 neurons; balanced coverage for N=10,000 <code>W_init</code> <code>'random'</code> Prototypes initialised from random data points <code>rho_0</code> 10.0 Initial bandwidth \u2248 half the lattice diameter <code>rho_f</code> 1.0 Final bandwidth = single-neighbour influence <code>target_epochs</code> 100 Bandwidth half-life; \u03c1_f reached at epoch 200 <code>n_epochs</code> 200 Total training epochs"},{"location":"gtsom_mnist_tutorial/#next-steps","title":"Next steps\u00b6","text":"<ul> <li>Data-driven topology: <code>GTSOM.from_data</code> builds the output lattice via Delaunay triangulation of prototype positions in PCA or Laplacian Eigenmap space, allowing the topology to adapt to the data rather than being fixed in advance.</li> <li>Larger grids: For exploratory analysis of large datasets, a 30\u00d730 or 40\u00d740 grid provides finer spatial resolution at the cost of longer training time.</li> <li>Parallelism: <code>n_jobs</code> in <code>compile()</code> controls the number of CPU threads used for prototype updates. On multi-core machines, <code>n_jobs=-1</code> (all cores) can reduce training time substantially for large N and d.</li> </ul>"}]}